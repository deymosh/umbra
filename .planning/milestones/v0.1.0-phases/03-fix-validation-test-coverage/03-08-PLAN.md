---
phase: 03-fix-validation-test-coverage
plan: 08
type: execute
wave: 4
depends_on: ["03-01", "03-03", "03-07"]
files_modified:
  - docs/KNOWN_ISSUES.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [VALID-02, VALID-03, VALID-04, VALID-05, VALID-09, VALID-18, VALID-22, VALID-26, VALID-33, VALID-35, VALID-38]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "LOG-30, LOG-38, LOG-49, LOG-52 each stay in docs/KNOWN_ISSUES.md at their current status and each carries the same one shared note explaining that a unit test for their class is currently impossible and cross-referencing the deferred test-seam work, pasted identically into all four (D-10)."
    - "None of those four entries was moved to docs/TODO.md — they are deployed, working fixes, not backlog items (D-10)."
    - "LOG-2, LOG-3, LOG-6, LOG-13, LOG-26 each stay in docs/KNOWN_ISSUES.md and each carries a restatement making it unambiguous that the entry awaits the user's own device pass, naming what specifically cannot be asserted without a running app (D-01 plus LOG-6's distinct database reason)."
    - "LOG-3's restatement names the already-existing aspect-ratio case as bonus coverage and LOG-2's states that the image-gate case is adjacent bonus coverage only — neither is presented as grounds for closing its entry (D-01)."
    - "LOG-4 stays in docs/KNOWN_ISSUES.md carrying whichever rationale the dispositions ledger actually recorded for it, and if that ledger recorded it as testable with a real-race test still owed, that fact is surfaced as an unplanned gap rather than smoothed over."
    - "All 38 Fix Validation checkboxes in .planning/REQUIREMENTS.md are checked and all 38 Phase 3 traceability rows read Complete, because every one of the 38 entries now has a recorded determination — the phase's first success criterion."
    - "The full unit-test suite and the lint task both pass at the end of the phase, on the tree that includes every test this phase added or extended."
    - "The phase's own record states explicitly that no emulator or instrumented run was performed anywhere in it."
    - statement: "The shared blocker note is one exact string repeated four times, so a later grep for it finds all four entries at once rather than four near-miss paraphrases (adjacency edge)."
      verification: backstop
    - statement: "Adding a bullet to an entry that stays does not alter its Status, Found, Where, Fix, or prose text, so the entry remains movable verbatim later once its blocker clears (reversibility edge)."
      verification: backstop
    - statement: "The requirement checkbox and its traceability row are flipped together for every id, so the two views of the same fact can never disagree (consistency edge)."
      verification: backstop
  artifacts:
    - docs/KNOWN_ISSUES.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "03-DISPOSITIONS.md supplies every sentence written into an entry here — the shared blocker note, LOG-4's rationale, and the five device-pass restatements are all pasted from it rather than composed against the live tracker."
    - "The four blocked entries share their blocker with the deferred test-seam item in docs/TODO.md; the cross-reference is the whole point of the shared note, so a future reader does not attempt an isolated test class before that work lands."
    - "REQUIREMENTS.md's Phase 3 rows are what a later phase and the milestone audit read to decide whether this phase actually closed; leaving them Pending after the work is done makes the phase look unfinished."
    - "The full-suite run is this phase's own fourth success criterion and is the last gate before the release-preparation phase begins on a tree it assumes is clean."
  prohibitions:
    - "Do not move any of these ten entries to docs/DONE.md or docs/TODO.md — every one of them stays where it is, annotated in place. (flagged-unverified)"
    - "Do not alter any of these entries' Status, Found, Where, or Fix bullets, or a single word of their prose bodies. (flagged-unverified)"
    - "Do not run, write, or attempt an emulator or instrumented test in this plan; device validation stays the user's own opt-in action. (flagged-unverified)"
    - "Do not compose a fresh rationale for any entry — every sentence written into the tracker comes verbatim from the dispositions ledger. (flagged-unverified)"
    - "Do not touch LOG-35, and do not modify or renumber any VALID-01..VALID-10 definition text or any BUG-, VERS-, REL-, or SKILL- line. (flagged-unverified)"
    - "Do not modify any production or test source file in this plan; a failing gate is reported, not patched here. (flagged-unverified)"
---

<objective>
Annotate in place the ten entries this phase deliberately leaves open — four blocked by an untestable class, one blocked pair of fixes, and five needing a human looking at a running app — then mark all 38 requirement ids complete and run the phase's own full-suite and lint gate.

Purpose: The phase forbids leaving any entry in ambiguous limbo. An entry that stays in the pending list without a stated reason is exactly that limbo, and it is what the previous milestone's 38-entry backlog was made of. This plan is what makes each remaining entry's reason visible on the entry itself, and what closes the phase's traceability and build gates.
Output: Ten annotated entries in `docs/KNOWN_ISSUES.md`, 38 completed requirement rows in `.planning/REQUIREMENTS.md`, and a recorded green full-suite and lint run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md
@.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate the five architecturally blocked entries — LOG-30, 38, 49, 52 and LOG-4</name>
  <files>docs/KNOWN_ISSUES.md</files>
  <read_first>
    - .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md, the "Blocked by an untestable class" section (the one exact shared sentence for the four session-manager entries, and LOG-4's own recorded disposition and rationale)
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, decisions D-10 and D-02
    - docs/KNOWN_ISSUES.md — the line ranges of the `### LOG-4`, `### LOG-30`, `### LOG-38`, `### LOG-49`, `### LOG-52` sections only, located via `grep -n '^### LOG-' docs/KNOWN_ISSUES.md`
    - docs/TODO.md — the deferred test-seam entry the shared note cross-references, to confirm it is still present and still carries the id the note names
    - .claude/CLAUDE.md, the "Bug tracking" section (entry bullet conventions and the rule that a triaged-out item is annotated in place rather than deleted)
  </read_first>
  <action>
Annotate five entries in place. None of them moves, and none of their existing bullets or prose changes.

For LOG-30, LOG-38, LOG-49, and LOG-52, add one new `- **Validation:**` bullet immediately after each entry's existing Fix bullet, carrying the shared blocker sentence from the dispositions ledger. Paste the identical string into all four — same wording, same punctuation, same cross-reference to the deferred test-seam item in `docs/TODO.md`. The sentence must say that a unit test for this class is currently impossible because its dependencies are concrete classes with no interface seam, that the seam work is tracked as that deferred item, and that a reader should not attempt an isolated test class for the class before it lands. Do not vary the four by adding entry-specific detail; the shared string is what makes all four findable in one grep, and each entry's own Fix bullet already carries its specifics.

For LOG-4, add the same kind of `- **Validation:**` bullet carrying whichever rationale the ledger recorded. If the ledger recorded the blocked outcome, the bullet says the fix is present in current source at the quoted lines, that the real-race test the phase intended is not currently possible because neither affected class can be instantiated in a plain unit test, and that the entry therefore stays alongside the other blocked entries. If the ledger instead recorded that a constructible path exists, the bullet says exactly that — the entry stays because a real-race test is still owed, not because one is impossible — and this plan's SUMMARY must flag it as an unplanned gap needing its own plan. Do not choose between those two outcomes yourself; read which one the ledger recorded and paste it.

Leave every one of the five entries at its existing status. These are deployed, working fixes whose only defect is that nothing can currently test them, which is a different thing from an unfixed bug and a different thing from a backlog item. Nothing here moves to the backlog file.
  </action>
  <reversibility rating="reversible">Adds one bullet per entry in a documentation file; the entries stay movable verbatim once their blocker clears.</reversibility>
  <verify>
    <automated>for n in 4 30 38 49 52; do test "$(grep -c "^### LOG-$n " docs/KNOWN_ISSUES.md)" = "1" || exit 1; done; test "$(grep -c '^- \*\*Validation:\*\*' docs/KNOWN_ISSUES.md)" = "5"; git status --porcelain app/src docs/TODO.md docs/DONE.md | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - All five entries are still present as headings in `docs/KNOWN_ISSUES.md` and absent from `docs/DONE.md` and `docs/TODO.md`.
    - Each of the five carries exactly one new `- **Validation:**` bullet placed immediately after its Fix bullet.
    - The four session-manager entries' Validation bullets are byte-identical to one another, and the string matches the shared sentence recorded in `03-DISPOSITIONS.md`.
    - The shared sentence contains a cross-reference to the deferred test-seam item in `docs/TODO.md`.
    - LOG-4's Validation bullet matches the disposition the ledger actually recorded, and if that disposition was the constructible one, the SUMMARY flags an unplanned gap.
    - `git diff docs/KNOWN_ISSUES.md` shows only added bullet lines for these five entries — no deletions and no modified existing lines.
    - `git status --porcelain app/src docs/TODO.md docs/DONE.md` is empty.
  </acceptance_criteria>
  <done>Every entry blocked by an architectural gap says so on itself, with one shared, greppable reason across the four that share a class.</done>
</task>

<task type="auto">
  <name>Task 2: Restate the five entries awaiting the user's own device pass — LOG-2, 3, 6, 13, 26</name>
  <files>docs/KNOWN_ISSUES.md</files>
  <read_first>
    - .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md, the "Needs the user's own device pass" section (each entry's reason line and its ready-to-paste restatement sentence, including LOG-3's quoted bonus-test method name and LOG-6's recorded dependency-absence check)
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, decision D-01 (a partial logic test is bonus coverage, never grounds for closing a visual entry)
    - docs/KNOWN_ISSUES.md — the line ranges of the `### LOG-2`, `### LOG-3`, `### LOG-6`, `### LOG-13`, `### LOG-26` sections only, plus lines 1-4 (the file's own convention header, which this task extends by one sentence)
    - .claude/CLAUDE.md, the Workflow section's opt-in rule for emulator and device testing
  </read_first>
  <action>
Annotate five entries in place, same mechanics as Task 1: one new `- **Validation:**` bullet immediately after each entry's Fix bullet, carrying that entry's ready-to-paste restatement sentence from the dispositions ledger, with no other line touched.

Each sentence must make two things unambiguous: that the entry is waiting on the user's own device pass rather than sitting undecided, and precisely what cannot be asserted without a running app. Paste them from the ledger rather than composing them.

Two carry an extra element the ledger already recorded. LOG-3's bullet names the existing aspect-ratio case by its exact backtick method name and states that it satisfies the bonus-coverage half of the decision while the rendered frame still needs eyes — it is not evidence the visual bug is fixed. LOG-2's bullet states that the existing image-gate case covers the gate's own acquire and release safety in isolation and is adjacent bonus coverage only, never proof the rendering bug is fixed. Neither sentence may be phrased so a later reader could mistake the bonus test for a closing citation.

LOG-6 carries a different reason from the other four and its sentence must keep that distinction: the fix is a database delete statement, the project has no in-memory database harness and no instrumented test for it, the fake used by the ingestion integration test returns a constant for that method without executing any statement, and only a real database can prove the statement deletes the right rows. It is not a visual bug and must not be lumped in as one.

Finally, extend the file's own header with one sentence stating that every remaining entry at applied-fix status now carries a Validation bullet naming what is blocking its closure — either the user's own device pass or an architectural test-seam gap — so a reader arriving cold understands why entries are sitting here rather than in the completed log. Add that sentence to the existing header paragraph area; do not restructure the header or alter its existing text.

Perform no emulator run, no instrumented test, and no device interaction of any kind in this task. Handing these five to the user is the entire point of the annotation.
  </action>
  <reversibility rating="reversible">Adds one bullet per entry plus one header sentence in a documentation file.</reversibility>
  <verify>
    <automated>for n in 2 3 6 13 26; do test "$(grep -c "^### LOG-$n " docs/KNOWN_ISSUES.md)" = "1" || exit 1; done; test "$(grep -c '^- \*\*Validation:\*\*' docs/KNOWN_ISSUES.md)" = "10"; test "$(grep -c '^### LOG-' docs/KNOWN_ISSUES.md)" = "11"; git status --porcelain app/src docs/DONE.md docs/TODO.md | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - All five entries are still present in `docs/KNOWN_ISSUES.md`, each with exactly one new `- **Validation:**` bullet after its Fix bullet.
    - The file now contains exactly ten Validation bullets in total and exactly eleven LOG headings — the ten annotated entries plus the one still-open entry.
    - Each of the five sentences names both the user's own device pass and the specific thing that cannot be asserted without a running app.
    - LOG-3's bullet contains a backtick-quoted method name and the word describing it as bonus coverage rather than proof; LOG-2's states its existing case is adjacent coverage only.
    - LOG-6's bullet gives the database reason and does not describe it as a visual bug.
    - The file header gained exactly one new sentence and no existing header text changed.
    - `git diff docs/KNOWN_ISSUES.md` shows only added lines in this task.
    - No emulator, instrumented test, or device command was executed.
  </acceptance_criteria>
  <done>Every entry still waiting on a human says so on itself, with the reason and the owner both explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Complete all 38 requirement rows and run the phase's full-suite and lint gate</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file — the 38 Fix Validation checkbox lines, the 38 Phase 3 traceability rows, the coverage block, the per-phase count line, and the footer date line)
    - .planning/ROADMAP.md, the Phase 3 Success Criteria block (the five criteria this task's gate closes)
    - .planning/phases/03-fix-validation-test-coverage/03-07-SUMMARY.md (the recorded totals: how many entries moved on test evidence, how many on a source read, and any held back as gaps)
    - .planning/phases/03-fix-validation-test-coverage/03-03-SUMMARY.md (any unplanned gap it flagged, which changes what this task may mark complete)
  </read_first>
  <action>
First reconcile, then mark. Read the two summaries named above and confirm the arithmetic balances: 28 entries closed into the completed log across this phase — 27 moved by the previous plan plus the one the tracer moved — and 10 annotated in place by Tasks 1 and 2, totalling the 38 in scope. If any entry was held back as a gap by an earlier plan, the closed count drops and the held-back count is the difference; record the actual numbers rather than these expected ones, and do not mark a held-back entry's requirement complete.

Then update `.planning/REQUIREMENTS.md` with scoped edits. Flip every Fix Validation checkbox from unchecked to checked for each of the 38 ids whose determination is recorded, and flip its matching traceability row's status from pending to complete. Flip both for the same id in the same pass so the checkbox and the row can never disagree.

Add one sentence to the Fix Validation section recording the outcome split in plain terms: how many entries were closed into the completed log on an executed test, how many on a direct source read, and how many remain in the issues file awaiting either the user's own device pass or an architectural test seam. State it as a fact about the bug tracker; do not cite a decision id, a plan number, or a planning document from inside this file's prose, matching the convention the existing text already follows.

Leave the coverage totals and the per-phase count line as they already stand — an earlier plan recomputed them and this task changes no id's phase assignment. Confirm they still read correctly rather than recomputing them blindly. Update the footer date line to today with a short reason.

Finally, run the phase gate on the whole tree: the full unit-test suite, then the lint task. Record in the SUMMARY the observed total test count, the lint outcome, and an explicit statement that no emulator or instrumented run was performed anywhere in this phase. If either command fails, stop and report the failure with its output — do not patch a production or test file to make it pass, because a failure at this point means an earlier plan's work regressed something and that needs its own diagnosis rather than a quick fix here.
  </action>
  <reversibility rating="reversible">Status flips in a planning document plus two read-only build commands; no code, schema, or published identifier changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest &amp;&amp; ./gradlew lintDebug &amp;&amp; test "$(grep -c '^- \[x\] \*\*VALID-' .planning/REQUIREMENTS.md)" = "38" &amp;&amp; test "$(grep -c '| Phase 3 | Complete |' .planning/REQUIREMENTS.md)" = "38"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest` passes over the full suite and `./gradlew lintDebug` succeeds with no new warnings; both outcomes and the observed test count are recorded in the SUMMARY.
    - `grep -c '^- \[x\] \*\*VALID-' .planning/REQUIREMENTS.md` returns 38 and no Fix Validation checkbox remains unchecked.
    - `grep -c '| Phase 3 | Complete |' .planning/REQUIREMENTS.md` returns 38 and no Phase 3 row remains pending.
    - The coverage block still reads 58 total, 58 mapped, 0 unmapped, with Phase 3 counted as 38.
    - The new outcome sentence's three numbers sum to 38 and match the totals recorded in the consumed summaries.
    - `git diff .planning/REQUIREMENTS.md` shows no change to any BUG-, VERS-, REL-, or SKILL- line and no change to any requirement's description text.
    - The SUMMARY states explicitly that no emulator or instrumented run was performed in this phase.
    - `git status --porcelain app/src` is empty.
  </acceptance_criteria>
  <done>Every one of the 38 entries has a recorded determination reflected in both views of the traceability record, and the phase's build gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Annotated entry → future maintainer | The Validation bullet is what stops a future reader from re-deriving a blocker or attempting a test that cannot work; a vague one wastes exactly the effort it exists to save |
| Requirement status → release decision | The Phase 3 rows are read by the release-preparation phase and the milestone audit as evidence this phase closed |
| Phase gate → tree cleanliness | The full-suite run is the last check before a phase that assumes a clean, tested tree |
| Phase scope → device validation | Device runs are the user's own opt-in action; nothing in this phase may perform one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08-01 | Repudiation | requirement rows marked complete | high | mitigate | The task reconciles the closed and annotated counts against two prior summaries before flipping anything, forbids marking a held-back entry complete, and requires the added outcome sentence's numbers to sum to the in-scope total. |
| T-03-08-02 | Spoofing | an annotated entry reading as validated | medium | mitigate | Annotations are pasted from the dispositions ledger and every entry keeps its existing applied-fix status, so an annotated entry can never be mistaken for one in the completed log; the two bonus-test entries carry explicit not-proof wording. |
| T-03-08-03 | Elevation of Privilege | scope creep into device runs | medium | mitigate | Both annotation tasks prohibit any emulator or instrumented command, and the final summary must state outright that none was run anywhere in the phase. |
| T-03-08-04 | Tampering | a failing gate patched away | medium | mitigate | The plan forbids editing a production or test file to make the gate pass and requires the failure to be reported with its output, so a late regression surfaces instead of being absorbed. |
| T-03-08-05 | Information Disclosure | annotation text | low | accept | The sentences describe class structure and test infrastructure gaps; no pubkey, relay URL, key material, or user content appears, and none of this text reaches a log. |
| T-03-08-SC | Tampering | npm/pip/cargo/Gradle installs | n/a | accept | This plan adds no dependency of any kind, so the package-legitimacy gate is not applicable and no unverified-package checkpoint is required. |

No production code path and no new attack surface: this plan edits two markdown files and runs two read-only build tasks.
</threat_model>

<verification>
- `docs/KNOWN_ISSUES.md` contains exactly eleven LOG headings and exactly ten Validation bullets, one per remaining applied-fix entry.
- The four session-manager entries' Validation bullets are byte-identical to one another.
- No entry moved to `docs/DONE.md` or `docs/TODO.md` in this plan; `git diff` on `docs/KNOWN_ISSUES.md` is additions only.
- `.planning/REQUIREMENTS.md` has 38 checked Fix Validation boxes and 38 complete Phase 3 rows, with the coverage block unchanged.
- `./gradlew testDebugUnitTest` and `./gradlew lintDebug` both pass on the full tree.
- No `.kt`, `.gradle.kts`, or resource file changed in this plan's commits, and no emulator or instrumented run occurred.
</verification>

<success_criteria>
- Every one of the ten entries left open states, on itself, why it is open and who closes it — none is left in ambiguous limbo.
- All 38 requirement ids are recorded complete in both the checkbox list and the traceability table, consistently with what actually happened to each entry.
- The phase ends on a green full suite and clean lint, with no device run anywhere in it.
</success_criteria>

## Artifacts this phase produces

New or changed symbols introduced by this plan (so drift verification does not flag them):

| Symbol | Kind | Location |
|--------|------|----------|
| `**Validation:**` bullet on LOG-2, 3, 4, 6, 13, 26, 30, 38, 49, 52 | 10 annotated bug-tracker entries | `docs/KNOWN_ISSUES.md` |
| one new header sentence explaining the Validation bullet | changed file header prose | `docs/KNOWN_ISSUES.md` |
| `VALID-01` .. `VALID-38` marked complete | 38 checkbox flips plus 38 traceability status flips | `.planning/REQUIREMENTS.md` |

No new production class, no new test class, no new test method, no new string resource, no new Gradle dependency.

<output>
Create `.planning/phases/03-fix-validation-test-coverage/03-08-SUMMARY.md` when done.
</output>

---
phase: 01-error-visibility-log-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt
  - docs/KNOWN_ISSUES.md
  - docs/TODO.md
  - docs/DONE.md
autonomous: true
requirements: [BUG-09, BUG-01, BUG-02, BUG-04, BUG-10, BUG-11]

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A logout failure triggered from the Settings screen logs the throwable at error level, matching the already-shipped fix on the feed screen's independent logout entry point (BUG-09, D-02)"
    - "Settings still navigates to the login screen after a failed logout — the added logging changes visibility only, never the navigation outcome (D-02)"
    - "No throwable-discarding handler and no unscrubbed relay URL, pubkey, or raw exception message remains at any of this phase's fifteen in-scope log sites"
    - "compileDebugKotlin, lintDebug, and the full testDebugUnitTest suite all pass on the phase's combined result, with no new warnings"
    - "Every one of this phase's newly-added error-level messages is a static description; the only interpolated values anywhere in an error-level lambda are pre-scrubbed"
    - "The bug tracker reflects reality: the five fixed KNOWN_ISSUES entries carry the applied-fix status, LOG-17 has moved into DONE.md, and the two out-of-scope gaps found while fixing are filed rather than lost"
  artifacts:
    - path: "app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt"
      provides: "File-scope tagged logger plus an error-level logout failure handler"
      contains: "settingsScreenLogger"
    - path: "docs/KNOWN_ISSUES.md"
      provides: "LOG-18, LOG-20, LOG-26, LOG-27, LOG-28 moved to applied-fix status with a Fix line each"
      contains: "fix applied — needs on-device validation"
    - path: "docs/DONE.md"
      provides: "LOG-17 moved verbatim from TODO.md with Completed and From lines"
      contains: "### LOG-17"
    - path: "docs/TODO.md"
      provides: "LOG-17 removed; LOG-32 and LOG-33 filed for the two gaps found but deliberately not fixed"
      contains: "### LOG-32"
  key_links:
    - from: "app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt"
      to: "app/src/main/java/com/umbra/app/util/logging/UmbraLog.kt"
      via: "file-scope logger obtained from the UmbraLog.tag factory, mirroring FeedScreen's declaration"
      pattern: "UmbraLog\\.tag\\("
    - from: "docs/KNOWN_ISSUES.md"
      to: "docs/DONE.md"
      via: "shared global LOG-N counter — no id reused, next free id is 32"
      pattern: "LOG-3[23]"
  prohibitions:
    - statement: "MUST NOT let the Settings logout-failure log record WHAT was being wiped or WHO was logged out. This is the second of two independent logout entry points; both fire at the moment a privacy-first user is deliberately destroying local state, and a log line naming the account or the relay set outlives the state it describes. The message must say that logout failed, nothing more."
      category: privacy
      requirement_id: BUG-09
      status: unresolved
      verification: null
      note: "Flagged-unverified — surfaced by the spec-less prohibition-recall pass (no SPEC.md exists for this phase). Task 1's acceptance criteria enforce the concrete half (static message, no scrub-requiring interpolation); the broader intent is recorded for human review, not auto-dismissed."
    - statement: "MUST NOT mark a KNOWN_ISSUES entry validated, or move it to DONE.md, on the strength of anything other than a real on-device run by the user. Umbra's tracker deliberately distinguishes 'fix applied' from 'validated' precisely so a passing unit test cannot silently be read as a confirmed working app. Applying the applied-fix status is the ceiling for this phase."
      category: transparency
      requirement_id: BUG-09
      status: unresolved
      verification: null
      note: "Flagged-unverified — same spec-less prohibition-recall pass. Task 3's acceptance criteria enforce the concrete half (no Validated line added, no entry moved out of KNOWN_ISSUES.md)."
  flagged_assumptions:
    - "BUG-09 / edge-probe category `idempotency`, status `unresolved` — 'what happens if this runs twice on the same input?' has a partial real referent: Settings and Feed are two independent logout entry points, so a user could conceivably trigger logout from both. The existing in-flight guard on the Settings handler predates this phase and is unchanged by it, so the question is recorded rather than resolved by new work."
    - "BUG-09 / edge-probe category `concurrency`, status `unresolved` — 'if interrupted or run in parallel, what is guaranteed?' maps to the same two-entry-point situation above. Pre-existing coroutine-scoped behavior, untouched by this phase. Recorded, not resolved."
    - "Phase-wide, BUG-01/02/04/10/11 / edge-probe categories `unclassified`, `empty`, `encoding`, `adjacency`, `ordering`, all status `unresolved` — carried forward from Plans 01-01 and 01-02, where each is recorded with its own reading. None was silently dropped; none is claimed resolved."
---

<objective>
Close the phase: fix the last of the six requirements' code sites, prove the whole phase's result against the project's own log-hygiene audit and full build gates, and bring the bug tracker into line with what actually shipped.

Purpose: `SettingsScreen.kt` is the second, independent logout entry point — the same bug the feed screen already had fixed. Its fix is the last code change in the phase, which makes this the right place to run a single phase-wide audit sweep rather than eleven per-task ones, and to do the bookkeeping ROADMAP Success Criterion 5 requires. Two gaps were found while implementing but are deliberately outside the locked scope of D-02 and BUG-02; they get filed as backlog entries here rather than silently fixed or silently forgotten.

Output: a file-scope logger and an error-level logout handler in `SettingsScreen.kt`; a green `compileDebugKotlin` / `lintDebug` / `testDebugUnitTest` triple plus a clean `find-non-lambda-logs` sweep across all ten touched files; five `docs/KNOWN_ISSUES.md` entries advanced to applied-fix status; `docs/TODO.md`'s LOG-17 moved verbatim into `docs/DONE.md`; two new backlog entries, LOG-32 and LOG-33.

Requirements covered: BUG-09 in full (code plus bookkeeping). BUG-01, BUG-02, BUG-04, BUG-10, and BUG-11 appear in this plan's `requirements` because their bug-tracker closeout lands here — their code fixes shipped in Plans 01-01 and 01-02, and ROADMAP Success Criterion 5 makes the tracker state part of this phase's definition of done, so those requirements are not complete until this plan runs.
Decisions applied: D-02 (log-only, no user-facing surface on the wipe path), D-01 (error level, no manual re-scrub).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-error-visibility-log-hygiene/01-CONTEXT.md
@.planning/phases/01-error-visibility-log-hygiene/01-RESEARCH.md
@.planning/phases/01-error-visibility-log-hygiene/01-01-SUMMARY.md
@.planning/phases/01-error-visibility-log-hygiene/01-02-SUMMARY.md
@AUDIT.md
@.claude/skills/find-non-lambda-logs/SKILL.md

Both prior SUMMARYs are referenced because Task 3's `**Fix:**` lines and LOG-17's DONE.md entry must describe what actually landed across all three plans, and Task 2's audit sweep must know which fifteen sites were in scope.

**Build toolchain.** In each new shell: `export JAVA_HOME="$(pwd)/toolchain/jdk-17"` and `export PATH="$JAVA_HOME/bin:$PATH"`. If `toolchain/jdk-17` does not exist, run `scripts/install-toolchain.sh` first (idempotent, Linux only).

**Bug tracker rules** (from `.claude/CLAUDE.md`): a single global `LOG-N` counter is shared across `docs/KNOWN_ISSUES.md`, `docs/TODO.md`, and `docs/DONE.md`; ids are never reused. The highest id currently in use anywhere is LOG-31, so LOG-32 and LOG-33 are the next two free. A fixed-but-unvalidated bug stays in KNOWN_ISSUES.md with an updated status — it does NOT move to DONE.md until a real device run confirms it. A shipped backlog item does move to DONE.md.
</context>

<!-- planner-discipline-allow: catch (_: Exception) -->

<tasks>

<task type="auto">
  <name>Task 1: Settings logout failure handler and file-scope logger</name>
  <precondition>toolchain/jdk-17/bin/java exists and local.properties names an Android SDK (established when Plan 01-01 bootstrapped the build toolchain)</precondition>
  <files>app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt</files>
  <read_first>
    - `app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` lines 1-46 (import block, ending around line 36; there is no file-scope declaration between the imports and the `SettingsScreen` composable at line 43-44 today) and lines 191-212 (the logout `MenuItemRow`, whose `onClick` launches a coroutine with an in-flight `isLoggingOut` guard, a handler that discards its exception around line 201-204, and an unconditional navigation to the login route afterward).
    - `app/src/main/java/com/umbra/app/ui/feed/FeedScreen.kt` line 101 (`private val feedScreenLogger = UmbraLog.tag("FeedScreen")` — the file-scope declaration shape to mirror) and lines 383-401 (the already-shipped sibling fix: bound exception, `feedScreenLogger.e(e) { "Logout failed" }`, navigation still unconditional afterward).
    - `app/src/main/java/com/umbra/app/util/logging/UmbraLog.kt` — the `tag(...)` factory, the only entry point.
  </read_first>
  <action>
    Add `import com.umbra.app.util.logging.UmbraLog` to `SettingsScreen.kt`'s import block, and declare a file-scope `private val settingsScreenLogger = UmbraLog.tag("SettingsScreen")` between the imports and the `SettingsScreen` composable, mirroring `FeedScreen.kt` line 101's placement and shape exactly. Do not add a `TAG` constant — the feed screen's precedent passes the tag string inline at the file-scope declaration, and this file must match its sibling.

    Change the logout handler inside the logout `MenuItemRow`'s `onClick` so the caught exception is bound and passed to `settingsScreenLogger.e(e) { "Logout failed" }` — the same message text the feed screen already uses. Keep everything else in that coroutine byte-identical: the `isLoggingOut` guard and its reset, the unconditional navigation to the login route with its `popUpTo(0) { inclusive = true }`, and the absence of any user-facing message. D-02 locks this to log-only: do not add a snackbar, a dialog, an error state, or an aggregated "logout completed with warnings" surface, and do not make navigation conditional on success.

    Pass a static description string only. The wrapper appends the scrubbed throwable text itself, so do not interpolate the exception, and do not name the account, the relay set, or any row count — a logout-path log must record that logout failed, not what was being erased. If you add a code comment, state the reason directly (a failed logout must not be indistinguishable from a successful one) rather than citing a planning document, a decision id, or a commit hash.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
    <automated>test "$(grep -c 'settingsScreenLogger' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt)" = "2"</automated>
    <automated>test "$(grep -c '_: *Exception' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt)" = "0"</automated>
    <automated>test "$(grep -c 'import com.umbra.app.util.logging.UmbraLog' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt)" = "1"</automated>
    <automated>./gradlew lintDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'settingsScreenLogger' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` returns exactly `2` — the file-scope declaration and the one call site.
    - `grep -c 'private val settingsScreenLogger = UmbraLog.tag("SettingsScreen")' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` returns `1`, declared outside any composable function body.
    - `grep -c '_: *Exception' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` returns exactly `0`.
    - `grep -c 'settingsScreenLogger.e(e) { "Logout failed" }' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` returns `1` — the message is a static string with no interpolation.
    - `git diff app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` shows no change to the `isLoggingOut` guard or its reset, no change to the navigation call or its `popUpTo`, and no added snackbar, dialog, or error-state composable.
    - `grep -c 'const val TAG' app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` returns `0` — the inline-tag shape matches the feed screen's precedent.
    - `./gradlew compileDebugKotlin` and `./gradlew lintDebug` both succeed with no warnings.
    - No code comment added by this task contains a phase, plan, task, or decision identifier, or a commit hash.
  </acceptance_criteria>
  <done>Settings logout failures log the throwable at error level through a file-scope tagged logger; navigation and in-flight guarding are unchanged; compile and lint are clean.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-wide log-hygiene audit sweep and full build gate</name>
  <files>(no files modified — verification only; report findings in the SUMMARY)</files>
  <read_first>
    - `.claude/skills/find-non-lambda-logs/SKILL.md` — the two audit checks and their exact search patterns; this task is that skill applied to the phase's whole diff.
    - `AUDIT.md` — the authoritative logging and scrubbing rules the sweep enforces; re-read the logging section if a finding is ambiguous rather than guessing.
    - `.planning/phases/01-error-visibility-log-hygiene/01-01-SUMMARY.md` and `01-02-SUMMARY.md` — the definitive list of which sites were changed and which were deliberately left alone.
  </read_first>
  <action>
    Run the project's full verification triple in order and require all three green: `./gradlew compileDebugKotlin`, then `./gradlew lintDebug` (CI treats warnings as errors, so any warning is a failure), then `./gradlew testDebugUnitTest` across the whole suite — not just the three test classes this phase added. The `LogoutUseCase` constructor gained a parameter, so a suite-wide run is the only thing that proves no other test file broke.

    Then run the two audit checks from the `find-non-lambda-logs` skill across the entire main source set, not only the ten touched files, and record the results in the SUMMARY:
    Check 1 — search for a `logger` or `client.logger` debug/warn call whose lambda body interpolates a relay URL, url, pubkey, or npub without a `scrub` call on the same line. Every hit must be manually confirmed either scrubbed or out of this phase's scope; a hit that is neither is a regression this phase introduced and must be fixed before the plan completes.
    Check 2 — search for a handler or failure callback whose only logging is a debug/warn call referencing the caught exception's message. Zero such hits may remain in the ten files this phase touched.

    Then run the error-level hygiene assertion described in the verify block: no error-level call anywhere in the main source set may interpolate a value that is not already scrubbed. Confirm by inspection that the one error-level message carrying bare-variable interpolation (the WebSocket-failure message, which embeds a relay local that was scrubbed at its assignment and an HTTP status suffix) is the only such case, and that the interpolated relay value is genuinely the scrubbed local rather than the raw parameter.

    Finally, count the phase's total error-level call sites across the main source set and confirm the expected total: two pre-existing plus twenty-four added by this phase. If the count differs, identify which site is missing or extra before proceeding. Modify no source file in this task; if the sweep finds a real regression, fix it as an explicit correction and say so in the SUMMARY rather than silently absorbing it.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin && ./gradlew lintDebug && ./gradlew testDebugUnitTest</automated>
    <automated>test -z "$(grep -rn 'logger\.e(' app/src/main/java/com/umbra/app/ --include=*.kt | grep -F '${' | grep -v 'scrub')"</automated>
    <automated>test "$(grep -rh 'logger\.e(' app/src/main/java/com/umbra/app/ --include=*.kt | wc -l)" = "26"</automated>
    <automated>test -z "$(grep -rnE '(logger|client\.logger)\.(d|w) *\{[^}]*\$\{?(relayUrl|url|pubkey|npub)' app/src/main/java/com/umbra/app/ --include=*.kt | grep -v 'scrub')"</automated>
    <automated>test "$(grep -rc '_: *Exception' app/src/main/java/com/umbra/app/domain/usecase/LogoutUseCase.kt app/src/main/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCase.kt app/src/main/java/com/umbra/app/ui/auth/LoginViewModel.kt app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt app/src/main/java/com/umbra/app/data/repository/EventRepositoryImpl.kt | grep -c ':0$')" = "4"</automated>
    <human-check>On-device validation of the five fixed KNOWN_ISSUES entries is deliberately NOT performed here. Per `.claude/CLAUDE.md`, emulator and device runs are opt-in and Umbra does not run autonomous on-device passes. Task 3 leaves those five entries in KNOWN_ISSUES.md at applied-fix status awaiting the user's own `run-umbra` pass; nothing in this phase launched an emulator.</human-check>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileDebugKotlin`, `./gradlew lintDebug`, and `./gradlew testDebugUnitTest` all exit zero; `lintDebug` reports zero warnings.
    - `grep -rn 'logger\.e(' app/src/main/java/com/umbra/app/ --include=*.kt | grep -F '${' | grep -v 'scrub'` produces no output — no error-level lambda interpolates an unscrubbed value.
    - `grep -rh 'logger\.e(' app/src/main/java/com/umbra/app/ --include=*.kt | wc -l` returns `26`: two pre-existing in `EventRepositoryImpl`, plus 5 (`TrimMemoryCachesUseCase`) + 7 (`LogoutUseCase`) + 2 (`PublishEventUseCases`) + 5 (`LoginViewModel`) + 1 (`UmbraNostrClient`) + 1 (`RelayMessageHandling`) + 1 (`RelayWebSocketListener`) + 1 (`EventRepositoryImpl` wipe path) + 1 (`SettingsScreen`) added by this phase.
    - The Check-1 pattern returns no hit lacking a `scrub` call on the same line, across the whole main source set.
    - The Check-2 pattern returns no hit inside the ten files this phase touched.
    - Five of the six files that had underscore-bound discarding handlers now report zero — `LogoutUseCase.kt` is the deliberate exception and still reports exactly `1` (its method-wide handler, out of D-02's scope).
    - `git status --porcelain app/src/main/` shows no modification attributable to this task unless a real regression was found, in which case the SUMMARY names the regression, the file, and the correction.
    - The SUMMARY records the Check-1 and Check-2 output verbatim, and states explicitly that no emulator or device was launched.
  </acceptance_criteria>
  <done>All three build gates pass; both `find-non-lambda-logs` checks are clean across the touched files; the error-level site count matches the expected 26; results recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Bug tracker closeout — five statuses advanced, LOG-17 shipped, two gaps filed</name>
  <files>docs/KNOWN_ISSUES.md, docs/TODO.md, docs/DONE.md</files>
  <read_first>
    - `docs/KNOWN_ISSUES.md` — the five entries to update: LOG-18 (three unscrubbed log messages), LOG-20 (silent handler during the wipe sequence), LOG-26 (Settings logout swallowed exception), LOG-27 (per-step cleanup catches discard every failure), LOG-28 (session-activation failures swallowed). Note the file's existing entry shape: a `- **Status:**` line, a `- **Found:**` line, a `- **Where:**` line, then prose.
    - `docs/TODO.md` — the LOG-17 entry (`Publish failure logs drop the throwable and emit at debug level`), which must move verbatim into DONE.md, plus the entry-shape template above it that must be left alone.
    - `docs/DONE.md` — the append-only log. Read the last two entries for the exact trailer format: a `**Completed:** <date>` line, and for backlog-sourced entries a `**From:** TODO LOG-<n>` line. The LOG-25 entry near the end is the closest analog (a logout-swallowed-exception fix) and shows how a `**Fix:**` line is phrased descriptively rather than as a bare hash.
    - `.planning/phases/01-error-visibility-log-hygiene/01-01-SUMMARY.md` and `01-02-SUMMARY.md` — the source of truth for what each `**Fix:**` line should describe.
    - `.claude/CLAUDE.md` "Bug tracking" section — the shared-counter rule and the applied-fix-versus-validated distinction.
  </read_first>
  <action>
    In `docs/KNOWN_ISSUES.md`, change the `- **Status:**` line of LOG-18, LOG-20, LOG-26, LOG-27, and LOG-28 from the open value to `fix applied — needs on-device validation`, and add a `- **Fix:**` line immediately after each. Write each Fix line to describe what actually changed, in terms a reader understands without opening a planning document or diffing a commit — for example, for LOG-27: the two cleanup use cases now take a constructor-injected logger and each of their twelve per-step handlers logs the exception at error level. Do not add a `**Validated:**` line to any of them and do not move any of them to DONE.md: a passing unit test is not a device run, and the tracker's distinction between applied and validated is the whole point of leaving them here. Leave the eight other open entries untouched.

    Move the LOG-17 entry from `docs/TODO.md` into `docs/DONE.md` verbatim — same heading, same body text — appending a `**Completed:** 2026-09-02` line and a `**From:** TODO LOG-17` back-reference, matching the trailer format the file's existing backlog-sourced entries use. Append it at the end of DONE.md; the file is an append-only historical record, so edit no existing entry. Delete the LOG-17 section from `docs/TODO.md` but leave the entry-shape template above it in place.

    File two new backlog entries in `docs/TODO.md` for the gaps found while implementing but deliberately left unfixed, using the file's own backlog entry shape (`- **Status:** backlog`, `- **Added:** 2026-09-02`, `- **Why:** ...`, then a description). LOG-32: `LogoutUseCase`'s method-wide handler still discards its exception, and its final user-preferences clear call is the one cleanup step with no handler of its own — so a failure there falls into that silent method-wide handler. Note that this sits deliberately outside the seven-site scope the phase's logging fix was locked to. LOG-33: `NegentropySyncOrchestrator`'s sync-aborted debug log interpolates a relay-supplied reason string raw, without routing it through the message-scrubbing helper that the file's other relay-sourced values use — a real scrubbing gap of the same class as the three the phase just fixed, but not one of the three that requirement's scope named.

    Use exactly LOG-32 and LOG-33: the shared global counter across all three files currently tops out at LOG-31, and ids are never reused.
  </action>
  <verify>
    <automated>test "$(for id in 18 20 26 27 28; do awk -v k="### LOG-$id " 'index($0,k)==1{f=1;next} f&&/^- \*\*Status:\*\*/{print;exit}' docs/KNOWN_ISSUES.md; done | grep -c 'fix applied — needs on-device validation')" = "5"</automated>
    <automated>test "$(grep -c '\*\*Status:\*\* open' docs/KNOWN_ISSUES.md)" = "8"</automated>
    <automated>test "$(grep -c '\*\*Validated:\*\*' docs/KNOWN_ISSUES.md)" = "0"</automated>
    <automated>test "$(grep -c '^### LOG-17' docs/TODO.md)" = "0" && test "$(grep -c '^### LOG-17' docs/DONE.md)" = "1"</automated>
    <automated>test "$(grep -c '^\*\*From:\*\* TODO LOG-17' docs/DONE.md)" = "1"</automated>
    <automated>test "$(grep -c '^### LOG-32' docs/TODO.md)" = "1" && test "$(grep -c '^### LOG-33' docs/TODO.md)" = "1"</automated>
    <automated>test -z "$(grep -oE 'LOG-(3[4-9]|[4-9][0-9])' docs/TODO.md docs/KNOWN_ISSUES.md docs/DONE.md)"</automated>
  </verify>
  <acceptance_criteria>
    - Each of LOG-18, LOG-20, LOG-26, LOG-27, LOG-28 in `docs/KNOWN_ISSUES.md` has a Status line reading exactly `fix applied — needs on-device validation` and a following `- **Fix:**` line; the awk-per-entry check returns `5`.
    - Each of those five `**Fix:**` lines describes the actual change (which classes, which handlers, what they now do) and contains no phase, plan, task, or decision identifier and no commit hash — it is readable with `.planning/` deleted.
    - `grep -c '**Validated:**' docs/KNOWN_ISSUES.md` returns `0` and `grep -c '^### LOG-' docs/KNOWN_ISSUES.md` returns the same value as before this task — no entry was moved out, so nothing was closed on test evidence alone.
    - `grep -c '**Status:** open' docs/KNOWN_ISSUES.md` returns `8` (thirteen before, minus the five advanced).
    - LOG-17 appears exactly once in `docs/DONE.md` and zero times as a heading in `docs/TODO.md`; its DONE.md entry carries both a `**Completed:**` line and a `**From:** TODO LOG-17` line.
    - `git diff docs/DONE.md` shows only appended lines — no existing DONE.md entry was edited.
    - `docs/TODO.md` contains new `### LOG-32` and `### LOG-33` headings, each with a Status, Added, and Why line, and each describing the gap plus why it was deliberately not fixed in this phase.
    - No id above LOG-33 appears in any of the three files — the shared counter advanced by exactly two.
  </acceptance_criteria>
  <done>Five KNOWN_ISSUES entries carry applied-fix status with descriptive Fix lines; LOG-17 has shipped into DONE.md with its trailers; LOG-32 and LOG-33 are filed; no entry was validated or closed without a device run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app process → Android logcat | The Settings logout handler is promoted from silent to error level, which on a release build means emitted-by-default rather than suppressed-by-default. |
| user's encrypted state → wipe-path log records | Settings logout is the second of two entry points that destroy local state; a log line written at that moment outlives the state it describes. |
| implementation reality → bug tracker | `docs/KNOWN_ISSUES.md` is what the maintainer and future contributors trust about which bugs are live. A status that overstates confidence is a durable false claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | New Settings logout-failure error log | medium | mitigate | The message is the static string `Logout failed` with no interpolation; the throwable text is scrubbed once inside the wrapper. Verified by the exact-string grep in Task 1's acceptance criteria. |
| T-01-12 | Information Disclosure | Any error-level message added anywhere in the phase | medium | mitigate | Task 2's sweep asserts that no error-level lambda in the entire main source set interpolates an unscrubbed value, and that the total error-site count matches exactly — an unexpected extra site fails the gate rather than passing unexamined. |
| T-01-13 | Repudiation | The five KNOWN_ISSUES entries advanced by Task 3 | medium | mitigate | Status advances only to applied-fix, never to validated; no entry moves to DONE.md. Enforced by the zero-Validated-line and unchanged-entry-count criteria, so the tracker cannot claim device confirmation this phase never obtained. |
| T-01-14 | Tampering | Two out-of-scope gaps found during implementation | low | mitigate | Filed as LOG-32 and LOG-33 rather than silently fixed (scope creep) or silently dropped (lost finding). Verified by the heading-presence and counter-ceiling gates. |
| T-01-SC | Tampering | npm/pip/cargo/Gradle dependency installs | high | accept | No package is installed by this plan; `app/build.gradle.kts` is not in `files_modified`. RESEARCH.md's Package Legitimacy Audit records "not applicable — this phase installs no external packages." |
</threat_model>

<artifacts_this_phase_produces>
This plan adds one production symbol; the phase's full net-new surface is:

| Artifact | Kind | Location | Created by |
|---|---|---|---|
| `settingsScreenLogger` — file-scope tagged logger val | new production symbol | `app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt` | Plan 01-03 (this plan) |
| `FakeUmbraLogger` (with nested `Call` data class and `errorCalls` accessor) | new test double | `app/src/test/java/com/umbra/app/testutil/fakes/FakeUmbraLogger.kt` | Plan 01-01 |
| `logger: UmbraLogger` — new trailing constructor parameter on `LogoutUseCase` | new production dependency | `app/src/main/java/com/umbra/app/domain/usecase/LogoutUseCase.kt` | Plan 01-01 |
| `logger: UmbraLogger` — new trailing constructor parameter on `TrimMemoryCachesUseCase` | new production dependency | `app/src/main/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCase.kt` | Plan 01-01 |
| `UmbraLog.tag("UmbraLogout")` argument in `provideLogoutUseCase` | new Hilt `@Provides` wiring | `app/src/main/java/com/umbra/app/di/UseCaseModule.kt` | Plan 01-01 |
| `UmbraLog.tag("UmbraTrimMemory")` argument in `provideTrimMemoryCachesUseCase` | new Hilt `@Provides` wiring | `app/src/main/java/com/umbra/app/di/UseCaseModule.kt` | Plan 01-01 |
| `TrimMemoryCachesUseCaseTest`, `PublishEventUseCasesTest` | new test classes | `app/src/test/java/com/umbra/app/domain/usecase/` | Plan 01-01 |

No other production symbol is created by Phase 1. Every remaining change in the phase is a call-site edit or an import adjustment inside an existing class.
</artifacts_this_phase_produces>

<verification>
- `./gradlew compileDebugKotlin`, `./gradlew lintDebug`, and `./gradlew testDebugUnitTest` all pass on the phase's combined result.
- The `find-non-lambda-logs` skill's Check 1 and Check 2 both come back clean across the ten touched files.
- `grep -rh 'logger\.e(' app/src/main/java/com/umbra/app/ --include=*.kt | wc -l` returns 26.
- Every underscore-bound discarding handler in the phase's scope is gone except `LogoutUseCase`'s method-wide one, which is deliberately retained and filed as LOG-32.
- `docs/KNOWN_ISSUES.md`, `docs/TODO.md`, and `docs/DONE.md` reflect exactly what shipped, with no entry validated or closed on non-device evidence.
</verification>

<success_criteria>
- BUG-09's code fix has landed and matches its already-shipped sibling on the feed screen.
- All fifteen of the phase's in-scope log sites are fixed and the phase-wide audit sweep is clean.
- The full build triple passes with zero lint warnings.
- ROADMAP Success Criterion 5 is met: LOG-18, LOG-20, LOG-26, LOG-27, LOG-28 carry applied-fix status with a Fix line each, and LOG-17 has moved verbatim into DONE.md with its Completed and From lines.
- The two out-of-scope gaps found while implementing are filed as LOG-32 and LOG-33 rather than fixed or forgotten.
- No emulator or device was launched at any point in this phase.
</success_criteria>

<output>
Create `.planning/phases/01-error-visibility-log-hygiene/01-03-SUMMARY.md` when done.
</output>

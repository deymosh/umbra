---
phase: 03-fix-validation-test-coverage
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md
autonomous: true
requirements: [VALID-02, VALID-03, VALID-04, VALID-05, VALID-09, VALID-11, VALID-13, VALID-18, VALID-20, VALID-22, VALID-26, VALID-27, VALID-33, VALID-34, VALID-35, VALID-37]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of LOG-18, LOG-20, LOG-28, LOG-39, LOG-51, LOG-54 has a recorded source-read verification quoting the current file and line where the logging call now sits, plus the quoted declaration proving that class builds its logger internally rather than receiving it as a constructor parameter (VALID-11, 13, 20, 27, 34, 37 — D-09's third disposition)."
    - "Each of LOG-30, LOG-38, LOG-49, LOG-52 is recorded as staying in docs/KNOWN_ISSUES.md with one shared architectural-blocker note cross-referencing the deferred NostrSessionManager test-seam work, and none of them is moved to docs/TODO.md (VALID-22, 26, 33, 35 — D-10)."
    - "The NostrSessionManager blocker claim is verified in this plan, not assumed: the ledger quotes that class's constructor parameter list and the declaration of each dependency showing it is a concrete class with no interface seam and no fake in the shared test doubles directory."
    - "LOG-4's disposition is decided from evidence recorded here: the ledger quotes UserRepositoryImpl's constructor, quotes ImagePrefetcher's constructor showing it requires an Android Context and a Coil ImageLoader, and records whether the project has any Robolectric or mocking dependency that would make that constructible in a JVM unit test (VALID-04)."
    - "LOG-2, LOG-3, LOG-6, LOG-13, LOG-26 are recorded as staying in docs/KNOWN_ISSUES.md awaiting the user's own device pass, each with a one-line reason naming what specifically cannot be asserted without a running app (VALID-02, 03, 05, 09, 18)."
    - "LOG-6's reason is backed by a recorded check that the build defines no Robolectric, no Room in-memory test harness, and no instrumented DAO test — so the SQL in the superseded-revision delete statement has no execution path in a JVM test."
    - "No entry in this plan is recorded as needing an emulator run performed by this phase; every eyeball entry is explicitly handed to the user."
    - statement: "Re-reading any quoted file:line after an unrelated later edit may shift the line number; the ledger therefore quotes the matching source text alongside the line number so the citation stays checkable if lines move (adjacency edge)."
      verification: backstop
    - statement: "LOG-51 sits in both the blocked-class group and the non-injected-logger group; it is recorded once, under the source-read disposition, because that is the disposition its own decision assigned it (ordering edge)."
      verification: backstop
    - statement: "If the LOG-4 blocker check comes out the other way and a JVM-constructible path exists, the entry is recorded TESTABLE and surfaced as an unplanned gap rather than silently downgraded to a source-read citation (idempotency-of-judgment edge)."
      verification: backstop
  artifacts:
    - .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md
  key_links:
    - "03-DISPOSITIONS.md is the second of the two inputs Plan 03-07 consumes — it supplies both the source-read rationale strings for the entries that move to DONE.md and the restatement text for the entries that stay."
    - "The blocked-class group shares its blocker with the deferred NostrSessionManager/RelayConfigViewModel coverage item in docs/TODO.md; the shared note exists so a future reader does not attempt an isolated test class before that seam work happens."
    - "The non-injected-logger pattern is what separates these entries from the two cleanup use cases Plan 03-06 extends, which do take a logger as a constructor parameter and therefore are testable."
  prohibitions:
    - "Do not write, run, or attempt an emulator or instrumented test in this plan; device validation stays the user's own opt-in action. (flagged-unverified)"
    - "Do not attempt to construct a test fixture for any class recorded as blocked — the blocker is architectural and the workaround is out of this phase's scope. (flagged-unverified)"
    - "Do not record a source-read verification without quoting the actual current source text; a line number alone is not a verification. (flagged-unverified)"
    - "Do not relocate any entry to docs/TODO.md — the blocked entries are working, deployed fixes, not backlog items. (flagged-unverified)"
    - "Do not modify any production or test source file in this plan. (flagged-unverified)"
---

<objective>
Decide and record, with quoted evidence, the disposition of the 16 entries that cannot be closed by a unit test: the six logging fixes behind a non-injected logger, the four blocked by an untestable session-manager class, LOG-4's own blocked pair of fixes, and the five that genuinely need a human looking at a running app.

Purpose: The phase forbids leaving any entry undecided, and equally forbids pretending an unassertable fix has test evidence. This plan produces the durable, checkable rationale for every entry that will never carry a test citation — verified against current source rather than taken from research on trust.
Output: `.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md` — one row per entry with disposition, quoted evidence, and the exact rationale text Plan 03-07 writes into the bug tracker.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md
@.planning/phases/03-fix-validation-test-coverage/03-RESEARCH.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source-read verification for the six non-injected-logger fixes — LOG-18, 20, 28, 39, 51, 54</name>
  <files>.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, decision D-09 (the exact third-disposition wording and its required rationale shape)
    - .planning/phases/03-fix-validation-test-coverage/03-RESEARCH.md, "Critical Finding 2" and the Group D section
    - docs/KNOWN_ISSUES.md — the `### LOG-18`, `### LOG-20`, `### LOG-28`, `### LOG-39`, `### LOG-51`, `### LOG-54` sections
    - app/src/main/java/com/umbra/app/data/repository/EventRepositoryImpl.kt — the logger field declaration, the two relay-URL log sites, and `clearAllData`'s disconnect catch
    - app/src/main/java/com/umbra/app/data/repository/NegentropySyncOrchestrator.kt — the logger field declaration and the NIP-77 sync-failure catch
    - app/src/main/java/com/umbra/app/ui/auth/LoginViewModel.kt — the logger field declaration and both inner session-activation catches
    - app/src/main/java/com/umbra/app/ui/relay/RelayConfigViewModel.kt — the logger field declaration and `enforceAnonymousRelayPolicyIfNeeded`
    - app/src/main/java/com/umbra/app/data/nostr/NostrSessionManager.kt — the logger field declaration and the two onFailure handlers
    - app/src/main/java/com/umbra/app/ui/common/InteractionActionsCoordinator.kt — the logger field declaration and the two sites named in LOG-54
    - app/src/main/java/com/umbra/app/util/logging/Logger.kt and UmbraLog.kt — the isLoggable gating that makes these calls unobservable in a JVM test
    - app/build.gradle.kts, the `testOptions` block (the returnDefaultValues setting that makes the Android log methods no-ops under unit test)
  </read_first>
  <action>
Create `.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md` with a header explaining that it records every entry that cannot be closed by a unit test, and three sections: "Source-read verified (moves to DONE.md)", "Blocked by an untestable class (stays in KNOWN_ISSUES.md)", and "Needs the user's own device pass (stays in KNOWN_ISSUES.md)". Use a table per section with columns `LOG-N | Requirement | Disposition | Evidence (file:line + quoted source) | Rationale text for the tracker`.

Fill the first section with six rows. For each entry, do two source reads and quote both:
first, the class's logger field declaration, to show it is built inside the class from the tagged-logger factory rather than received as a constructor parameter — quote the declaration line verbatim with its file and line number;
second, the actual fixed call site named in that entry's `**Fix:**` bullet, to show the change is present in current source — quote the line with its file and line number. For LOG-18 that is three separate sites across two files; quote all three.

Write each row's Rationale text as the exact sentence Plan 03-07 will paste into the tracker, following D-09's shape: state that the fix was verified by direct source read, that the class builds its logger internally so no unit test can intercept the call, and give the file and line. The sentence must make clear that no test exists — do not phrase it so a reader could mistake it for a test-backed entry.

Add one paragraph under the section recording why a test is impossible in general here, citing the returnDefaultValues test option and the absence of any mocking framework in the build file, so the six rows share one architectural explanation instead of repeating it.
  </action>
  <reversibility rating="reversible">Creates one planning document; no source or tracker file is touched.</reversibility>
  <verify>
    <automated>test -f .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md && for n in 18 20 28 39 51 54; do grep -q "LOG-$n" .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md || exit 1; done && git status --porcelain app/src | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `03-DISPOSITIONS.md` exists with a "Source-read verified" section containing exactly six rows, one each for LOG-18, LOG-20, LOG-28, LOG-39, LOG-51, LOG-54.
    - Every row quotes both a logger-field declaration and the fixed call site, each with a `path:line` reference and the verbatim source text.
    - LOG-18's row quotes three call sites across two files.
    - Every row's rationale sentence contains the phrase `verified by direct source read` and explicitly states that no unit test can assert the call.
    - The shared explanation paragraph names the unit-test option in `app/build.gradle.kts` that makes the Android logging calls no-ops, and states that the project has no mocking framework.
    - `git status --porcelain app/src` is empty.
  </acceptance_criteria>
  <done>Six unassertable logging fixes have quoted, checkable source-read verifications and ready-to-paste rationale text.</done>
</task>

<task type="auto">
  <name>Task 2: Verify and record the two architectural blockers — LOG-30, 38, 49, 52 and LOG-4</name>
  <files>.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, decision D-10 (these four stay in KNOWN_ISSUES.md with one shared note) and decision D-02 (the real-race test LOG-4 was supposed to get)
    - .planning/phases/03-fix-validation-test-coverage/03-RESEARCH.md, "Critical Finding 3", the Group C section, and Assumption A2 in the Assumptions Log (the untested part of the blocker claim this task must actually check)
    - docs/KNOWN_ISSUES.md — the `### LOG-4`, `### LOG-30`, `### LOG-38`, `### LOG-49`, `### LOG-52` sections
    - docs/TODO.md — the LOG-44 entry (the deferred test-seam work the shared note cross-references)
    - app/src/main/java/com/umbra/app/data/nostr/NostrSessionManager.kt — the class declaration and full constructor parameter list
    - app/src/main/java/com/umbra/app/data/tor/TorRuntimeManager.kt and the BackfillAnchorStore declaration — the class-versus-interface question
    - app/src/main/java/com/umbra/app/data/repository/UserRepositoryImpl.kt — the class declaration and full constructor parameter list, and `saveRelayList`
    - app/src/main/java/com/umbra/app/util/ImagePrefetcher.kt lines 20-35 (its constructor)
    - app/src/test/java/com/umbra/app/testutil/fakes/ (the full list of available shared fakes)
    - app/build.gradle.kts and gradle/libs.versions.toml (to confirm whether any mocking or Robolectric dependency exists)
  </read_first>
  <action>
Append the "Blocked by an untestable class" section to `03-DISPOSITIONS.md` with five rows.

First verify the session-manager blocker rather than assuming it. Quote that class's constructor parameter list with its file and line, then for each dependency that is not an interface quote its own declaration line showing it is a concrete class. Cross-check the shared fakes directory listing and record that no fake exists for those dependencies. Record the conclusion as an explicit finding, and note the residual limitation honestly: the check is a declaration-level one, not an attempted compile.

Record LOG-30, LOG-38, LOG-49, LOG-52 as staying in `docs/KNOWN_ISSUES.md` at their current status. Compose one shared note — the same sentence for all four — stating that a unit test for this class is currently impossible because its dependencies are concrete classes with no interface seam, that the seam work is tracked as the deferred coverage item in `docs/TODO.md`, and that a reader should not attempt an isolated test class for it before that lands. Put the exact sentence in the ledger so Plan 03-07 pastes it identically into all four entries.

Then verify LOG-4's blocker. That entry carries two distinct fixes: a reactive fourth input to the session manager's relay reconciliation combine, and the atomic staleness check-and-write plus post-lock freshness re-validation in the user repository. Quote the user repository's constructor parameter list, then quote the image-prefetcher constructor showing it requires an Android context and a Coil image loader. Record whether the build declares any Robolectric or mocking dependency, and record the result of `grep -rl "android.content.Context" app/src/test/` as evidence of whether any existing JVM unit test constructs an Android context at all. Also quote the atomic compute call in `saveRelayList` to confirm the fix itself is present in current source.

If that evidence shows the class cannot be constructed in a JVM unit test, record LOG-4's disposition as blocked, note that its first fix lives in the same session-manager class as the four above, and write its tracker rationale as: the fix is present in current source at the quoted lines, the real-race test the phase intended for it is not currently possible because neither class can be instantiated in a plain JVM unit test, and the entry therefore stays in `docs/KNOWN_ISSUES.md` alongside the other blocked entries.

If instead the evidence shows a JVM-constructible path exists, do not write a source-read rationale. Record the disposition as `TESTABLE — real-race test still owed`, state exactly which constructor arguments make it constructible, and flag it in this plan's SUMMARY as an unplanned gap that needs its own plan, because writing that test is outside this plan's scope.
  </action>
  <reversibility rating="reversible">Records findings in a planning document; no code path, no tracker entry, and no deferred item is moved.</reversibility>
  <verify>
    <automated>grep -q 'Blocked by an untestable class' .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md && for n in 4 30 38 49 52; do grep -q "LOG-$n" .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md || exit 1; done && git status --porcelain app/src docs | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The blocked section contains five rows: LOG-4, LOG-30, LOG-38, LOG-49, LOG-52.
    - The session-manager constructor parameter list is quoted verbatim with a `path:line`, and each non-interface dependency has its own quoted declaration line.
    - The ledger records the shared blocker note as one exact sentence, and states that it is to be pasted identically into all four of LOG-30, LOG-38, LOG-49, LOG-52.
    - No row instructs moving an entry to `docs/TODO.md`.
    - LOG-4's row quotes the user repository constructor, the image-prefetcher constructor, the atomic compute call in the relay-list save, the result of the Android-context grep over the test sources, and the presence or absence of a mocking or Robolectric dependency.
    - LOG-4's disposition is exactly one of `BLOCKED` with a rationale sentence, or `TESTABLE — real-race test still owed` with an explicit unplanned-gap flag recorded in the SUMMARY.
    - `git status --porcelain app/src docs` is empty.
  </acceptance_criteria>
  <done>Both architectural blockers are verified against current source rather than assumed, and all five affected entries have recorded dispositions.</done>
</task>

<task type="auto">
  <name>Task 3: Record the five device-validation entries — LOG-2, LOG-3, LOG-6, LOG-13, LOG-26</name>
  <files>.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md</files>
  <read_first>
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, decision D-01 (a partial logic test is bonus coverage, never grounds for closing a visual entry)
    - docs/KNOWN_ISSUES.md — the `### LOG-2`, `### LOG-3`, `### LOG-6`, `### LOG-13`, `### LOG-26` sections
    - app/src/test/java/com/umbra/app/util/ImageLoadGateTest.kt (method names — the adjacent coverage LOG-2 may cite as bonus only)
    - app/src/test/java/com/umbra/app/ui/components/media/VideoPlayerControllerTest.kt (the anamorphic pixel-ratio case — LOG-3's bonus coverage, already present)
    - app/src/main/java/com/umbra/app/ui/components/UserAvatar.kt — the retry schedule constants and the effect they live in, to confirm no pure function exists to test
    - app/src/main/java/com/umbra/app/ui/settings/SettingsScreen.kt — the logout click handler LOG-26 fixed, to confirm it is composable-only code with no view model seam
    - app/src/main/java/com/umbra/app/data/db/dao/EventDao.kt — the superseded-replaceable delete query LOG-6 added
    - app/build.gradle.kts and gradle/libs.versions.toml — to confirm the absence of Robolectric and of any Room testing artifact
    - .claude/CLAUDE.md, the Workflow section's opt-in rule for emulator and device testing
  </read_first>
  <action>
Append the "Needs the user's own device pass" section to `03-DISPOSITIONS.md` with five rows.

For each entry, record a one-line reason naming precisely what cannot be asserted without a running app, and the restatement sentence Plan 03-07 will add to the tracker entry making it unambiguous that the entry is awaiting the user's own device pass rather than sitting in limbo.

LOG-2: the fix is the consolidation of the image-permit lifecycle into a single effect inside a composable; record that the existing gate test covers the gate's own acquire and release safety in isolation and may be cited as adjacent bonus coverage only, never as proof the rendering bug is fixed.
LOG-3: record that the pure aspect-ratio computation behind this fix already has a passing case in the video-player controller test — quote its exact backtick method name — and that this satisfies the bonus-test half of the decision with no new work, while the rendered-frame behaviour still needs eyes.
LOG-13: record that the retry schedule is inline inside a composable effect using composable-local state, with no pure function to extract, and that attempting one would require a production-code change outside this phase's scope; recommend no bonus test.
LOG-26: record that the fixed catch is in composable click-handler code with no view model, and that the project has no Compose UI test or Robolectric infrastructure.
LOG-6: record the distinct reason — the fix is a Room DAO delete statement, the project has no Robolectric, no Room in-memory test harness, and no instrumented DAO test, and the fake DAO used by the ingestion integration test returns a constant for that method without executing SQL. Quote the dependency-absence check you ran. State that only a real database can prove the statement deletes the right rows.

Close the file with a summary line stating the total entry count recorded across all three sections and the split by disposition, and an explicit statement that no emulator or device run was performed by this phase.
  </action>
  <reversibility rating="reversible">Appends rows to a planning document only.</reversibility>
  <verify>
    <automated>for n in 2 3 6 13 26; do grep -q "LOG-$n" .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md || exit 1; done && grep -q 'no emulator or device run' .planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md && git status --porcelain app/src docs | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The device-pass section contains exactly five rows: LOG-2, LOG-3, LOG-6, LOG-13, LOG-26.
    - Every row carries both a reason line and a ready-to-paste restatement sentence that names the user's own device pass.
    - LOG-3's row quotes the exact backtick method name of the existing anamorphic aspect-ratio case.
    - LOG-2's row states that the gate test is adjacent bonus coverage only and is not grounds for closing the entry.
    - LOG-6's row records the executed dependency-absence check and states that only a real database can prove the delete statement.
    - `03-DISPOSITIONS.md` totals 16 entry rows across its three sections, and its closing line states that no emulator or device run was performed.
    - `git status --porcelain app/src docs` is empty.
  </acceptance_criteria>
  <done>All five device-validation entries are recorded with precise reasons and restatement text, and the dispositions ledger is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source read → permanent tracker claim | A source-read rationale replaces test evidence in the durable record; if the quoted source does not actually show the fix, the claim is false |
| Research assumption → executor conclusion | The blocker claims arrive as untested assumptions and become the reason four working fixes stay open |
| Phase scope → device validation | Device runs are the user's own opt-in action; nothing in this phase may perform one |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03-01 | Repudiation | source-read rationale rows | high | mitigate | Every rationale must quote current source text alongside a `path:line`, and the sentence must state outright that no test exists — so a later reader can re-verify it and cannot mistake it for test-backed evidence. |
| T-03-03-02 | Spoofing | blocker claims taken on trust | medium | mitigate | Task 2 re-derives both blockers from quoted constructor declarations, the shared fakes listing, and a dependency check, and records the residual limitation that the check is declaration-level rather than an attempted compile. |
| T-03-03-03 | Elevation of Privilege | scope creep into device runs | medium | mitigate | The device-pass entries are recorded as handed to the user; the plan prohibits running an emulator or instrumented test and the ledger must close with an explicit statement that none was run. |
| T-03-03-04 | Information Disclosure | quoted source lines | low | accept | Quoted lines are log-call and constructor declarations; the sites in question exist precisely to scrub relay URLs and throwable text, and no pubkey, relay URL, or user content is copied into the ledger. |
| T-03-03-SC | Tampering | npm/pip/cargo/Gradle installs | n/a | accept | No dependency is added; the package-legitimacy gate does not apply. |

No production code changes and no new attack surface: this plan reads source and writes one planning document.
</threat_model>

<verification>
- `.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md` contains 16 entry rows across three sections and no entry appears twice.
- Every source-read row carries a `path:line` and verbatim quoted source text.
- `git status --porcelain app/src docs` is empty across all three tasks.
- No instrumented or emulator test was executed.

</verification>

<success_criteria>
- Every entry that will never carry a test citation has a recorded, checkable reason and ready-to-paste tracker text.
- Both architectural blockers are verified from source rather than inherited as assumptions, with LOG-4's outcome decided on evidence either way.
</success_criteria>

## Artifacts this phase produces

New or changed symbols introduced by this plan (so drift verification does not flag them):

| Symbol | Kind | Location |
|--------|------|----------|
| `03-DISPOSITIONS.md` | new phase artifact — non-test disposition ledger consumed by Plan 03-07 | `.planning/phases/03-fix-validation-test-coverage/03-DISPOSITIONS.md` |

No new production class, no new test class, no new test method, no new Gradle dependency.

<output>
Create `.planning/phases/03-fix-validation-test-coverage/03-03-SUMMARY.md` when done.
</output>

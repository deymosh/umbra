---
phase: 03-fix-validation-test-coverage
plan: 06
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt
  - app/src/test/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCaseTest.kt
  - app/src/test/java/com/umbra/app/domain/model/EventModelBehaviorTest.kt
  - app/src/test/java/com/umbra/app/ui/feed/FeedStateMergeCoordinatorTest.kt
autonomous: true
requirements: [VALID-06, VALID-19]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the logout use case's six remaining per-step cleanup catches — session stop, user clear, contact-list clear, mute-list clear, pin-list clear, and the backfill-anchor wipe — has a case proving the thrown instance itself reaches the injected logger, not merely that the wipe continued (VALID-19 / LOG-27, LogoutUseCase half)."
    - "Each of the cache-trim use case's four remaining per-step catches — user prune, contact-list trim, mute-list trim, pin-list trim — has the identical injected-logger assertion (VALID-19 / LOG-27, TrimMemoryCachesUseCase half)."
    - "Every one of the ten new cases asserts identity of the thrown instance against the recorded error call, so a catch that swallows and rethrows a fresh exception, or logs nothing, fails the case — the assertion is what the pre-fix silent catch could not satisfy."
    - "No new case asserts only that the remaining cleanup steps still ran; that behaviour predates the fix and passes identically against the pre-fix code, so it is not evidence (research Pitfall 3)."
    - "The future-timestamp check's no-argument form behaves identically to its explicit zero-tolerance form across past, present, and future offsets, pinning the hardcoded zero-tolerance product decision the existing parametric cases leave unpinned (VALID-06 / LOG-7, tolerance half)."
    - "A future-dated note delivered through the feed's notes flow is absent from the computed visible set while a past-dated note in the same emission is present, proving the future check is wired into the feed's own merge stage rather than only existing on the domain model (VALID-06 / LOG-7, wiring half)."
    - "A note whose own timestamp is in the past but whose repost timestamp is in the future is also excluded from the same computed visible set, covering the second future check in that merge stage."
    - "Both use case classes take their logger as a constructor parameter, so the recording logger fake substitutes cleanly — no test in this plan attempts to intercept a class-internal tagged logger (D-09 boundary)."
    - statement: "Adding a throw option to the four existing recording repository doubles leaves their default construction unchanged, so the three pre-existing cache-trim cases still pass byte-for-byte unmodified (adjacency edge)."
      verification: backstop
    - statement: "Each new logger case makes exactly one step throw, so the recorded error count of one distinguishes the step under test from every other step in the same run (isolation edge)."
      verification: backstop
    - statement: "The future-timestamp offsets are chosen far enough from the current second that a scheduling pause between reading the clock and calling the check cannot flip the expected result (flakiness edge)."
      verification: backstop
  artifacts:
    - app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt
    - app/src/test/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCaseTest.kt
    - app/src/test/java/com/umbra/app/domain/model/EventModelBehaviorTest.kt
    - app/src/test/java/com/umbra/app/ui/feed/FeedStateMergeCoordinatorTest.kt
  key_links:
    - "The constructor-injected logger is the whole reason these two use cases are testable where the six non-injected-logger classes in Plan 03-03 are not — the identity assertion against the recording fake is the seam, and it exists only here."
    - "The feed's merge stage combines the notes flow, the active filters flow, and the recheck ticker; a combine emits nothing until every source has emitted, so a computed snapshot arriving at all is itself proof the ticker is a live source of that flow."
    - "The two remaining ticker call sites, in the profile and thread view models, share the same combine idiom but sit behind constructors too heavy to fixture in this phase; the merge-stage case stands in for them structurally and the source-read record says so plainly."
    - "Plan 03-07 reads this plan's SUMMARY for the exact method names it cites on the logout/cleanup entry and the future-dated-events entry — a paraphrased name there becomes an uncheckable citation in the permanent record."
  prohibitions:
    - "Do not modify any production source file in this plan; every fix under test already shipped and this plan only adds coverage. (flagged-unverified)"
    - "Do not rename, delete, or alter the assertions of any pre-existing case in the four touched files — other plans and this project's own history cite them by exact name. (flagged-unverified)"
    - "Do not write a case whose only assertion is that later cleanup steps still ran; that shape passes against the pre-fix code and is not evidence. (flagged-unverified)"
    - "Do not build a fixture for the profile or thread view model in this plan — their constructor cost is out of scope and the merge-stage case plus a recorded source read is the agreed substitute. (flagged-unverified)"
    - "Do not introduce a mocking framework, a Robolectric dependency, a new shared fake, or any new Gradle dependency. (flagged-unverified)"
    - "Do not attempt to inject the recording logger fake into any class that builds its logger internally — that is Plan 03-03's territory and has no seam. (flagged-unverified)"
---

<objective>
Close the two coverage gaps the rest of this phase leaves open: the ten remaining per-step cleanup catches in the logout and cache-trim use cases that log through an injected logger but have no case asserting it, and the future-dated-events filter, which today is proved only as a domain-model function and never as something the feed actually applies.

Purpose: The phase's second success criterion is a named, passing test that would have failed against the pre-fix code. For the ten cleanup catches, the existing "the wipe continues anyway" cases pass identically before and after the fix, so they are not evidence — only an identity assertion against the injected logger is. For the future-dated-events entry, the existing cases are parametric over tolerance and prove nothing about the zero-tolerance default the product actually ships, nor that any feed path applies the check at all.
Output: Ten new logger-identity cases across the two use case test files, one default-tolerance case on the event model, and one wiring case on the feed merge coordinator — plus the exact method names recorded in the SUMMARY for Plan 03-07 to cite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md
@.planning/phases/03-fix-validation-test-coverage/03-PATTERNS.md
@.planning/codebase/TESTING.md

@app/src/main/java/com/umbra/app/domain/usecase/LogoutUseCase.kt
@app/src/main/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCase.kt
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: The logout use case's six remaining cleanup catches each reach the injected logger (LOG-27 / VALID-19, first half)</name>
  <files>app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt lines 255-405 (the five existing logout cases, the one existing logger-identity case at 362-387 whose shape every new case copies, and the throwing wrapper at 393-405 whose shape every new wrapper copies)
    - app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt lines 1-25 (the import block — every new wrapper needs its interface imported, and only `EventRepository` is imported today)
    - app/src/main/java/com/umbra/app/domain/usecase/LogoutUseCase.kt (whole file, 95 lines — the seven per-step catches, their order, the captured public key, and the guard that skips the backfill-anchor wipe when it is blank)
    - app/src/test/java/com/umbra/app/testutil/fakes/FakeUmbraLogger.kt (the recorded-call shape and the error-call filter the assertions read)
    - app/src/main/java/com/umbra/app/domain/nostr/NostrSessionController.kt (a two-method interface; its stop function is not suspending)
    - app/src/main/java/com/umbra/app/domain/repository/UserRepository.kt line 95, ContactListRepository.kt line 12, MuteListRepository.kt line 12, PinListRepository.kt line 13, EventRepository.kt line 63 (the exact signature of each remaining throwing site — all five are non-suspending, unlike the already-covered clear-all-data site)
    - .planning/phases/03-fix-validation-test-coverage/03-RESEARCH.md, the "Group E" section and Pitfall 3 (why the existing continue-anyway cases do not qualify as evidence)
  </read_first>
  <behavior>
    - Given the session controller's stop throws a specific instance, when logout runs, then the injected logger records exactly one error call carrying that same instance.
    - Given the user repository's clear-all throws, when logout runs, then the same single-error-call identity assertion holds.
    - Given the contact-list, mute-list, or pin-list clear-all throws, when logout runs, then the same holds for each independently.
    - Given a signed-in public key is present and the backfill-anchor wipe throws, when logout runs, then the same holds.
    - Given no step throws at all, the error-call list stays empty, so a spurious error record cannot masquerade as coverage.
  </behavior>
  <action>
Add six throwing wrapper classes and six cases to the existing file. Nothing in it is renamed, deleted, or re-asserted.

Write each wrapper as a file-private class using Kotlin interface delegation, exactly mirroring the existing `ThrowingClearAllDataEventRepository` at the bottom of the file: a `delegate` constructor parameter delegated with `by`, a `private val thrown: Throwable` parameter, and a single overridden member that throws it. Name them `ThrowingStopNostrSessionController` (overrides `stop`), `ThrowingClearAllUserRepository` (overrides `clearAll`), `ThrowingClearAllContactListRepository` (overrides `clearAll`), `ThrowingClearAllMuteListRepository` (overrides `clearAll`), `ThrowingClearAllPinListRepository` (overrides `clearAll`), and `ThrowingClearBackfillAnchorsEventRepository` (overrides `clearBackfillAnchors`). Place all six alongside the existing wrapper below the test class, and add the five interface imports the file does not yet carry. None of these six overrides is a suspending function — check each signature at the path given above rather than copying the suspending shape of the existing wrapper.

Write six cases inside the test class, following this file's own underscore-joined backtick naming, its plain `runBlocking` builder, and its practice of constructing the use case inline with shared fakes for every dependency other than the one under test. Name them exactly:
`given_nostrSessionControllerStopThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`,
`given_userRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`,
`given_contactListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`,
`given_muteListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`,
`given_pinListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`,
`given_clearBackfillAnchorsThrows_when_logging_then_loggerRecordsErrorWithSameThrowable`.

Each body builds one distinctly-messaged `IllegalStateException`, wraps the single dependency under test in its new wrapper, passes plain shared fakes for the other six dependencies plus a fresh recording logger, invokes the use case, and asserts the recorded error-call list has size one and that its throwable is the same instance — using the identity assertion, not equality. The single-error-count assertion is load-bearing: it is what proves the case isolated the step it names.

The backfill-anchor case needs one extra setup step the other five do not: the wipe is skipped entirely unless a public key was stored before the preferences were cleared, so seed the preferences fake with a 64-character key the way the existing anchor-clearing case at line 341 does. Without that seeding the case would record zero error calls and fail for the wrong reason.

Do not add a case that asserts the later cleanup steps still ran. Three such cases already exist in this file and none of them distinguishes the fixed code from the pre-fix code, because the best-effort continue-on-failure behaviour was never what changed.

If any new case records more than one error call, do not relax the assertion to a range — find which other dependency is also throwing under that fixture and use a non-throwing fake for it instead.
  </action>
  <reversibility rating="reversible">Adds test-only classes and cases; no production behaviour changes and no shared fake is modified.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.BackfillDeleteLogoutUseCaseTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.BackfillDeleteLogoutUseCaseTest"` passes with 21 cases executed (15 pre-existing plus 6 new).
    - All six new method names match the strings specified above character for character.
    - Each of the six new cases asserts a recorded error-call count of exactly 1 and asserts instance identity between the constructed throwable and the recorded one.
    - Six new file-private throwing wrappers exist, each implemented by interface delegation and overriding exactly one member.
    - The backfill-anchor case seeds a public key into the preferences fake before invoking the use case.
    - `git diff app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt` shows no modification to any pre-existing case body or method name.
    - `git status --porcelain app/src/main` is empty and `./gradlew compileDebugKotlin` succeeds.
  </acceptance_criteria>
  <done>All seven of the logout use case's per-step cleanup catches now have a case that fails if the logging call is removed, and the six new method names are recorded for citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The cache-trim use case's four remaining cleanup catches each reach the injected logger (LOG-27 / VALID-19, second half)</name>
  <files>app/src/test/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCaseTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCaseTest.kt (whole file, 148 lines — the five recording doubles at 25-80, the one existing logger-identity case at 84-102 whose shape the new cases copy, and the two cases at 104-147 that construct the four doubles with their current single-parameter constructors)
    - app/src/main/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCase.kt (whole file, 57 lines — the five per-step catches and their order)
    - app/src/main/java/com/umbra/app/domain/repository/UserRepository.kt line 105 (the prune function is suspending) and ContactListRepository.kt line 14, MuteListRepository.kt line 14, PinListRepository.kt line 15 (the three trim functions are not)
    - .planning/phases/03-fix-validation-test-coverage/03-PATTERNS.md, the `TrimMemoryCachesUseCaseTest.kt` section (the recording-wrapper shape and this file's distinct underscore naming convention)
  </read_first>
  <behavior>
    - Given the user repository's prune throws a specific instance, when the cache trim runs, then the injected logger records exactly one error call carrying that same instance.
    - Given the contact-list, mute-list, or pin-list trim throws, when the cache trim runs, then the same identity assertion holds for each independently.
    - Given none of them throws, the two existing cases that construct these doubles behave exactly as they do today.
  </behavior>
  <action>
Extend the four existing recording doubles rather than adding four more classes. Each of `RecordingUserRepository`, `RecordingContactListRepository`, `RecordingMuteListRepository`, and `RecordingPinListRepository` currently takes only a delegate and counts its one call. Give each a second constructor parameter, a nullable `Throwable` defaulting to null, and have the overridden member throw it after incrementing the counter — mirroring how `RecordingEventRepository` at the top of the same file already handles its own throw option. The default of null is what keeps the two existing cases at lines 104-147 compiling and passing unchanged; do not make the parameter required.

Add four cases inside the test class using this file's underscore-joined backtick naming, its `runBlocking` builder, and the existing logger-identity case at line 84 as the structural template. Name them exactly:
`given_userRepositoryPruneStaleDataThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable`,
`given_contactListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable`,
`given_muteListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable`,
`given_pinListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable`.

Each body builds one distinctly-messaged `IllegalStateException`, constructs only the double under test with that throwable while passing plain shared fakes for the other four dependencies, passes a fresh recording logger, invokes the use case once, and asserts a recorded error-call count of exactly one plus instance identity of the throwable. Invoke with the non-aggressive argument, matching the existing logger-identity case, so the two variants do not diverge for no reason.

Note that the prune step is a suspending function while the three trim steps are not — the overrides must keep each interface's own modifiers.

As in Task 1, do not add a case asserting only that the remaining steps still ran; one already exists in this file and it does not distinguish fixed from pre-fix code.
  </action>
  <reversibility rating="reversible">Adds an optional constructor parameter to four test-only doubles and four cases; no production behaviour changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.TrimMemoryCachesUseCaseTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.TrimMemoryCachesUseCaseTest"` passes with 7 cases executed (3 pre-existing plus 4 new).
    - All four new method names match the strings specified above character for character.
    - Each new case asserts a recorded error-call count of exactly 1 and asserts instance identity of the throwable.
    - The four extended doubles each gained exactly one nullable, null-defaulting constructor parameter, and the two pre-existing cases that construct them are unmodified in `git diff`.
    - `git status --porcelain app/src/main` is empty and `./gradlew compileDebugKotlin` succeeds.
  </acceptance_criteria>
  <done>All five of the cache-trim use case's per-step catches have a case that fails if the logging call is removed, completing the ten sub-sites this entry was missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The future-dated-events check is pinned at its shipped default and proved wired into the feed (LOG-7 / VALID-06)</name>
  <files>app/src/test/java/com/umbra/app/domain/model/EventModelBehaviorTest.kt, app/src/test/java/com/umbra/app/ui/feed/FeedStateMergeCoordinatorTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/domain/model/EventModelBehaviorTest.kt lines 179-207 (the two existing parametric cases, both of which always pass an explicit tolerance, and the `eventWithCreatedAt` helper they use)
    - app/src/main/java/com/umbra/app/domain/nip01/Event.kt lines 329-352 (the future check, its zero default, the strictly-greater comparison, and the bare-timestamp variant)
    - app/src/test/java/com/umbra/app/ui/feed/FeedStateMergeCoordinatorTest.kt (whole file, 240 lines — the subject factory at 84-104, the observable event-repository double at 64-82, the `event`/`noteView` helpers at 106-122, the real-dispatcher wait helper at 124-136, and the sequential-emissions case at 138-189 whose collect/emit/await/advance rhythm this new case copies exactly)
    - app/src/main/java/com/umbra/app/ui/feed/FeedStateMergeCoordinator.kt lines 206-250 (the three-source combine, the visible-note filter including both future checks, and the computed snapshot's event list)
    - app/src/main/java/com/umbra/app/domain/model/NoteView.kt lines 24-44 (the repost timestamp field the second future check reads)
    - app/src/main/java/com/umbra/app/ui/profile/ProfileViewModel.kt lines 292-320 and app/src/main/java/com/umbra/app/ui/feed/ThreadViewModel.kt lines 284-288 (the two other ticker call sites — read to quote them in the SUMMARY, not to build a fixture for either)
    - .planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md, the "Claude's Discretion" entry covering this entry's view-model wiring gap
  </read_first>
  <behavior>
    - Given events at past, current, and near-future offsets, when the future check is called with no argument and again with an explicit zero, then the two forms agree at every offset.
    - Given an event a minute ahead of the clock, when the future check is called with no argument, then it reports true; a minute behind reports false.
    - Given one past-dated note, one future-dated note, and one past-dated note carrying a future repost timestamp in a single feed emission, when the merge stage computes the visible set, then only the past-dated note survives.
  </behavior>
  <action>
First, the domain-model half. Add one case to the event-model file using its underscore-joined backtick naming, named exactly `given_noExplicitTolerance_when_checkingFuture_then_theDefaultBehavesAsZeroTolerance`. Read the clock once into a local seconds value, build events via the existing `eventWithCreatedAt` helper at four offsets — one minute behind, exactly the read value, five seconds ahead, and one minute ahead — and for each one capture the result of the explicit zero-argument call and the result of the no-argument call into two locals and assert they are equal. Then add two direct assertions: the one-minute-ahead event reports true with no argument, and the one-minute-behind event reports false with no argument. Repeat both direct assertions once against the bare-timestamp variant so the free function's own default is pinned too.

Use one minute, not one second, for the direct assertions. The check compares against a freshly-read clock on every call, so an offset of a second or two can flip between reading the clock and asserting; a minute cannot, while still failing immediately if the default were ever widened to a real clock-drift allowance. The equality assertions carry the exactness — they hold at every offset regardless of what the default is, and together with the existing explicit-tolerance cases they pin the default to zero without depending on timing at all.

Second, the wiring half. The feed's merge coordinator is where the home feed applies this check, and it already has a working test file that constructs it with fakes and drives emissions through a controllable notes flow — that is the seam this task uses. Extend its private `noteView` helper with two optional parameters, a creation timestamp defaulting to the current literal and a nullable repost timestamp defaulting to null, threading the first into the existing `event` helper's own parameter and the second into the note's repost timestamp field. Both defaults must leave the three existing cases behaving identically.

Add one case named exactly `given a future dated note in notesFlow when computedFeedFlow computes the visible set then it is excluded while a past dated note is kept`, following that file's space-separated backtick naming. Copy the sequential-emissions case's setup verbatim in shape: a replaying shared flow as the notes source, the background scope rather than the test scope for the coordinator, a collecting job on the computed flow, and the advance call before the first emission. Read the clock once, then emit a single result carrying three notes — one an hour behind, one an hour ahead, and one an hour behind but carrying a repost timestamp an hour ahead. Wait on the real-dispatcher helper, advance, then assert the visible-notes callback's last recorded list contains only the past-dated note's id, and assert the same against the computed flow's current snapshot event ids. Cancel the collecting job at the end, as the existing case does.

Give the new case a doc comment stating the invariant it protects: the merge stage combines the notes flow, the active filters flow, and the recheck ticker, so no snapshot can be produced at all unless the ticker has emitted — which is why this case also demonstrates the ticker is a live source of that flow rather than a dangling call. Describe the invariant only; do not name any plan, task, phase, decision id, requirement id, bug id, or commit in the comment.

Finally, record in this plan's SUMMARY, for Plan 03-07 to cite: the two new method names verbatim; and a source-read note quoting the ticker combine line and the future-check call in the profile view model and in the thread view model, each with its file and line, stating plainly that those two call sites are verified by direct source read rather than by a test because their constructors are too heavy to fixture in this phase, and that the merge-stage case covers the same idiom structurally.
  </action>
  <reversibility rating="reversible">Adds two cases and two optional helper parameters in test code; no production behaviour changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.domain.model.EventModelBehaviorTest" --tests "com.umbra.app.ui.feed.FeedStateMergeCoordinatorTest" &amp;&amp; ./gradlew compileDebugKotlin lintDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.model.EventModelBehaviorTest" --tests "com.umbra.app.ui.feed.FeedStateMergeCoordinatorTest"` passes, with the feed file executing 4 cases (3 pre-existing plus 1 new).
    - Both new method names match the strings specified above character for character.
    - The domain-model case contains at least four agreement assertions between the no-argument and explicit-zero forms, plus direct true/false assertions at the one-minute offsets on both the event method and the bare-timestamp function.
    - The feed case asserts that the visible-notes callback's last recorded list and the computed snapshot's event ids each contain exactly the past-dated note's id.
    - The feed case's emission contains three notes, one of which is past-dated with a future repost timestamp.
    - The extended `noteView` helper's new parameters both have defaults, and `git diff` shows the three pre-existing feed cases unmodified.
    - The new feed case's doc comment contains no plan, task, phase, decision, requirement, bug, or commit identifier.
    - `./gradlew lintDebug` succeeds with no new warnings and `git status --porcelain app/src/main` is empty.
    - The SUMMARY records both method names verbatim plus the quoted profile and thread view-model source-read lines with their file and line references.
  </acceptance_criteria>
  <done>The zero-tolerance default and the feed's application of the future check are both pinned by passing cases, and the two unfixtured call sites are recorded as source-read verified for Plan 03-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cleanup failure → operator visibility | The logout and cache-trim catches are the last place a wipe failure can be observed; a silent one means a user believes their data was deleted when it was not |
| Feed ingestion → rendered timeline | A relay can serve any creation timestamp it likes, so the future check is the only thing keeping a forged-future note pinned to the top of the feed |
| Test evidence → validated-fix claim | These cases become the citations that let two entries be recorded as validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Repudiation | logout/cache-trim cleanup catches | high | mitigate | Each of the ten new cases asserts instance identity between the thrown object and the recorded error call, and a recorded count of exactly one — an assertion a silently-swallowing catch cannot satisfy, unlike the continue-anyway cases the plan forbids reusing as evidence. |
| T-03-06-02 | Repudiation | a case that passes for the wrong reason | high | mitigate | The plan names the pre-fix-equivalent test shape explicitly and forbids it, and each acceptance criterion pins the executed case count so a case cannot be quietly dropped or replaced by a weaker one. |
| T-03-06-03 | Spoofing | forged future creation timestamp from a relay | medium | mitigate | The feed case proves the merge stage drops a future-dated note and a future repost timestamp from the computed visible set, and the domain-model case pins the tolerance at zero so no clock-drift window is silently reintroduced. |
| T-03-06-04 | Tampering | pre-existing cases in the four touched files | medium | mitigate | The plan forbids renaming or re-asserting any existing case, the four extended doubles keep null-defaulting parameters, and every task's acceptance criteria require the pre-existing bodies to be unmodified in the diff. |
| T-03-06-05 | Information Disclosure | test data and log assertions | low | accept | The cases use synthetic keys and example ids and assert only on throwable identity and note ids; no relay URL, real pubkey, or user content is introduced, and no assertion prints a scrubbed value. |
| T-03-06-06 | Denial of Service | a clock-sensitive case that flakes in CI | low | mitigate | The future offsets are a full minute from the read clock value and the exactness assertions are timing-independent equality checks, so no case depends on completing within a one-second window. |
| T-03-06-SC | Tampering | npm/pip/cargo/Gradle installs | n/a | accept | No dependency is added; the package-legitimacy gate does not apply. |

No production code changes and no new attack surface: this plan adds unit tests only.
</threat_model>

<verification>
- `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.BackfillDeleteLogoutUseCaseTest"` passes with 21 cases.
- `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.usecase.TrimMemoryCachesUseCaseTest"` passes with 7 cases.
- `./gradlew testDebugUnitTest --tests "com.umbra.app.domain.model.EventModelBehaviorTest" --tests "com.umbra.app.ui.feed.FeedStateMergeCoordinatorTest"` passes, the feed file with 4 cases.
- `./gradlew compileDebugKotlin` and `./gradlew lintDebug` both succeed with no new warnings.
- `git status --porcelain app/src/main` is empty across all three tasks.
- The twelve new method names are recorded verbatim in the plan SUMMARY so Plan 03-07 can cite them.
</verification>

<success_criteria>
- The logout/cleanup entry has a named, passing case for every one of its twelve per-step catches, each failing against the pre-fix silent-catch behaviour.
- The future-dated-events entry has a case pinning the shipped zero-tolerance default and a case proving the feed actually applies the check, with the two unfixtured call sites recorded as source-read verified rather than left unmentioned.
- No pre-existing case in any of the four touched files was renamed, weakened, or removed.
</success_criteria>

## Artifacts this phase produces

New or changed symbols introduced by this plan (so drift verification does not flag them):

| Symbol | Kind | Location |
|--------|------|----------|
| `ThrowingStopNostrSessionController` | new file-private test wrapper | `app/src/test/java/com/umbra/app/domain/usecase/BackfillDeleteLogoutUseCaseTest.kt` |
| `ThrowingClearAllUserRepository` | new file-private test wrapper | same file |
| `ThrowingClearAllContactListRepository` | new file-private test wrapper | same file |
| `ThrowingClearAllMuteListRepository` | new file-private test wrapper | same file |
| `ThrowingClearAllPinListRepository` | new file-private test wrapper | same file |
| `ThrowingClearBackfillAnchorsEventRepository` | new file-private test wrapper | same file |
| `given_nostrSessionControllerStopThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_userRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_contactListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_muteListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_pinListRepositoryClearAllThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_clearBackfillAnchorsThrows_when_logging_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_userRepositoryPruneStaleDataThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable` | new test method | `app/src/test/java/com/umbra/app/domain/usecase/TrimMemoryCachesUseCaseTest.kt` |
| `given_contactListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_muteListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| `given_pinListRepositoryTrimMemoryThrows_when_invoked_then_loggerRecordsErrorWithSameThrowable` | new test method | same file |
| optional throw parameter on `RecordingUserRepository`, `RecordingContactListRepository`, `RecordingMuteListRepository`, `RecordingPinListRepository` | changed file-private test doubles | same file |
| `given_noExplicitTolerance_when_checkingFuture_then_theDefaultBehavesAsZeroTolerance` | new test method | `app/src/test/java/com/umbra/app/domain/model/EventModelBehaviorTest.kt` |
| `given a future dated note in notesFlow when computedFeedFlow computes the visible set then it is excluded while a past dated note is kept` | new test method | `app/src/test/java/com/umbra/app/ui/feed/FeedStateMergeCoordinatorTest.kt` |
| optional creation-timestamp and repost-timestamp parameters on the private `noteView` helper | changed file-private test helper | same file |

No new production class, no new test class, no new shared fake, no new Gradle dependency.

<output>
Create `.planning/phases/03-fix-validation-test-coverage/03-06-SUMMARY.md` when done.
</output>

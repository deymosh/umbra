---
phase: 03-fix-validation-test-coverage
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt
autonomous: true
requirements: [VALID-10, VALID-25, VALID-30]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Saving a relay that is currently classified as discovered through the add/edit path persists it with the discovered flag cleared, on both the existing-id edit branch and the blank-id merge branch (VALID-10 / LOG-14)."
    - "A role removal overlapping a role setter on the SAME relay id serializes: while the setter's write is held open, the removal has not entered its own write, and once both resolve the relay carries the setter's new flag AND the removed role's cleared flag — neither update is lost (VALID-25 / LOG-37)."
    - "A relay save and a relay delete each overlapping a role setter on the same relay id both wait for that setter's write to complete instead of interleaving with it (VALID-30 / LOG-42)."
    - "Every new case forces a genuine overlap using the existing invocation gate rather than calling the two operations sequentially, matching the concurrency-test rigor this codebase already established for the same class."
    - "All new cases live in the existing RelayCrudCoordinatorTest file and reuse its existing subject factory and recording repository — no second fake and no new test file."
    - statement: "Applying the same role removal twice is a no-op on the second call: the first cleared the flags, the second maps an already-cleared relay and persists an equal value (idempotency edge)."
      verification: backstop
    - statement: "A save or delete for a relay id no other operation is touching acquires an uncontended lock and completes without waiting, so the new locking assertions do not imply cross-relay serialization (adjacency edge)."
      verification: backstop
  artifacts:
    - app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt
  key_links:
    - "The recording repository's invocation gate is what makes the overlap deterministic under virtual time — a test that merely calls two methods in sequence proves ordering, not locking."
    - "The role-removal path routes through the same private role-update chokepoint as the five per-role setters, which is why one lock covers all six operations."
    - "The relay save path has two distinct branches — blank id merges onto an existing row, non-empty id overwrites it — and the discovered-flag rule and the lock both have to hold on each."
  prohibitions:
    - "Do not modify any production source file in this plan; every fix under test already shipped and this plan only adds coverage. (flagged-unverified)"
    - "Do not rewrite, rename, or delete any of the five existing test cases in this file — the audit in Plan 03-02 cites them by exact method name. (flagged-unverified)"
    - "Do not replace a forced-overlap case with a sequential two-call case; asserting only final state after two ordered calls would pass against the pre-fix code. (flagged-unverified)"
    - "Do not introduce a mocking framework, a new fake class, or a new Gradle dependency. (flagged-unverified)"
---

<objective>
Extend `RelayCrudCoordinatorTest` with the coverage three already-shipped relay-coordinator fixes never received: the discovered-flag forcing on the manual save path, and the per-relay lock now held by the role-removal, save, and delete paths.

Purpose: These are the phase's three cheapest genuine coverage gaps — the class already has the strongest test harness in the codebase (a recording repository with a per-invocation gate that forces real overlap), so each missing case is a handful of lines against machinery that already exists. Once they pass, three more entries can move to the validated log on real evidence rather than a source read.
Output: Five new cases in the existing test file, and the exact method names Plan 03-07 cites for LOG-14, LOG-37, and LOG-42.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-fix-validation-test-coverage/03-CONTEXT.md
@.planning/phases/03-fix-validation-test-coverage/03-PATTERNS.md
@.planning/codebase/TESTING.md

@app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt
@app/src/main/java/com/umbra/app/ui/relay/RelayCrudCoordinator.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The manual save path clears the discovered flag on both branches (LOG-14 / VALID-10)</name>
  <files>app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt (whole file — the `sampleRelay` helper at 40-46, the `subject` factory at 48-67, the `RecordingRelayRepository` at 77-113, and the existing blank-id merge case at 209-234 which is the closest structural analog)
    - app/src/main/java/com/umbra/app/ui/relay/RelayCrudCoordinator.kt lines 53-150 (`saveRelay` — the sanitized copy that forces the discovered flag false at 81, the blank-id merge branch at 83-144 which sets it explicitly again at 118, and the non-empty-id branch at 145-150)
    - docs/KNOWN_ISSUES.md — the `### LOG-14` section, whose Where line still points at the view model rather than this coordinator
    - app/src/main/java/com/umbra/app/domain/relay/Relay.kt — the `isDiscovered` property and the default values of the role flags
  </read_first>
  <behavior>
    - Given a relay stored with the discovered flag set, when the add/edit save path is called with that relay's own id and an owned role enabled, then the persisted relay has the discovered flag cleared and the owned role set.
    - Given a relay stored with the discovered flag set, when the save path is called with a blank id and the same normalized url, then the single merged row has the discovered flag cleared.
    - Given the same blank-id save, then the repository still holds exactly one row for that url.
  </behavior>
  <action>
Add two `runTest` cases to `RelayCrudCoordinatorTest`, using the existing `subject(...)` factory and `RecordingRelayRepository` — do not add a new fake.

Name the first case exactly: `given a discovered relay when saveRelay assigns it an owned role then the persisted relay is no longer discovered`. Seed a relay via `sampleRelay(id = "r1", url = "wss://relay.example").copy(isDiscovered = true)`, build the subject with it, then call `coordinator.saveRelay(...)` passing that same relay's id with an owned role turned on — set `isWriteEnabled` and `isWriteActive` true on the copy handed to `saveRelay`. Call `advanceUntilIdle()`, then assert the stored relay read back through the repository has `isDiscovered == false` and `isWriteActive == true`.

Name the second case exactly: `given a discovered relay already in the repository when saveRelay is called with a blank id and the same url then the merged row is no longer discovered`. Seed the discovered relay directly into a `RecordingRelayRepository` and pass `relays = emptyList()` to `subject(...)`, mirroring the existing blank-id merge case's setup, then call `coordinator.saveRelay(Relay(id = "", url = "wss://relay.example", isEnabled = false, isReadEnabled = false, isWriteEnabled = true))`. After `advanceUntilIdle()`, assert the repository snapshot has size 1, and that the merged row has `isDiscovered == false` and `isWriteEnabled == true`.

Use `FakeUserPreferences(initialPubkey = "a".repeat(64))` — the factory's default — so the anonymous-session branch is not taken and the owned role is not stripped. Follow this file's existing space-separated backtick naming, plain JUnit assertions, and no-shared-state style. If you add a comment, describe the invariant (a relay given an owned role through the manual save path stops being classified as discovered) without naming any plan, task, phase, decision id, or commit.
  </action>
  <reversibility rating="reversible">Adds two test cases; no production behavior changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"` passes with seven cases executed (five pre-existing plus two new).
    - Both new method names match the two strings specified above character for character.
    - The first case asserts `isDiscovered == false` on a relay whose seeded value was `true`, so it fails if the discovered-forcing copy is removed from the save path.
    - The second case asserts both `isDiscovered == false` and a repository snapshot size of exactly 1.
    - `git status --porcelain app/src/main` is empty.
    - `./gradlew compileDebugKotlin` succeeds.
  </acceptance_criteria>
  <done>Both save branches are pinned to clear the discovered flag, and the two method names are recorded for citation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Role removal serializes against a role setter on the same relay (LOG-37 / VALID-25)</name>
  <files>app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt lines 156-207 (the two existing overlapping-toggle cases — the gate mechanism and the call-log assertions this task copies)
    - app/src/main/java/com/umbra/app/ui/relay/RelayCrudCoordinator.kt lines 222-262 (`removeRelayRole`, including the anonymous-session guard on the inbox and DM roles and the dirty-flag marking) and the private role-update chokepoint it now routes through
    - app/src/main/java/com/umbra/app/ui/relay/RelayRole.kt or the enum's declaration site (the exact role constant names)
    - docs/KNOWN_ISSUES.md — the `### LOG-37` section, whose Fix line names the racing case this test is supposed to add
  </read_first>
  <behavior>
    - Given a relay with the search role active, when a role setter's persist is held open and a removal of the search role is issued for the same relay id, then the removal has not entered its own persist while the setter's is held.
    - Given both then resolve, then the stored relay has the setter's new role flag set and the search role's flags cleared — neither write is lost.
  </behavior>
  <action>
Add one `runTest` case named exactly: `given removeRelayRole overlapping a role setter on the same relay when both resolve then neither the removal nor the flag flip is lost`.

Seed `sampleRelay(id = "relayA", url = "wss://relay.example").copy(isSearchEnabled = true, isSearchActive = true)` so the role being removed starts genuinely set and its clearing is an observable change. Build the subject with an explicit `RecordingRelayRepository`, then gate invocation index 0 exactly as the existing same-relay case does.

Call `coordinator.setOutboxEnabled("relayA", enabled = true)` first, then `coordinator.removeRelayRole("relayA", RelayRole.SEARCH)`, then `advanceUntilIdle()`. Assert the call log is exactly the single enter marker for that relay — the removal is blocked by the per-relay lock, not merely queued behind the gate. Complete the gate, `advanceUntilIdle()` again, then assert the call log shows two strictly non-overlapping enter/exit pairs and that the stored relay has `isWriteActive == true` and both `isSearchEnabled` and `isSearchActive` false.

Use the search role rather than the inbox or DM role: those two are rejected outright for an anonymous session, and this test's subject is a signed-in session, so choosing search keeps the case independent of that guard.
  </action>
  <reversibility rating="reversible">Adds one test case; no production behavior changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"` passes with eight cases executed.
    - The new method name matches the specified string character for character.
    - The case asserts a single enter marker while the gate is held — an assertion that fails if the removal path does not take the per-relay lock.
    - The case asserts the final relay has both the setter's flag set and the removed role's flags cleared.
    - `git status --porcelain app/src/main` is empty.
  </acceptance_criteria>
  <done>The role-removal path is pinned to serialize against the role setters on the same relay id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Save and delete serialize against a role setter on the same relay (LOG-42 / VALID-30)</name>
  <files>app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt</files>
  <read_first>
    - app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt lines 156-181 (the gate-and-call-log idiom) and 209-234 (the existing save case)
    - app/src/main/java/com/umbra/app/ui/relay/RelayCrudCoordinator.kt lines 145-150 (the non-empty-id save branch's locked write) and lines 180-220 (`deleteRelay`'s locked removal and the lock-map pruning that follows it)
    - docs/KNOWN_ISSUES.md — the `### LOG-42` section, including the note that the brand-new-relay branch is deliberately left unlocked
  </read_first>
  <behavior>
    - Given a role setter's persist is held open for a relay id, when a save is issued for that same id, then the save's persist has not entered while the setter's is held, and enters only after it completes.
    - Given a role setter's persist is held open for a relay id, when a delete is issued for that same id, then the row still exists while the setter's persist is held, and is gone once it completes.
  </behavior>
  <action>
Add two `runTest` cases.

Name the first exactly: `given saveRelay overlapping a role setter on the same relay id when both resolve then the second write waits for the first`. Seed `sampleRelay(id = "relayA", url = "wss://relay.example")`, gate invocation index 0, call `coordinator.setOutboxEnabled("relayA", enabled = true)` then `coordinator.saveRelay(sampleRelay(id = "relayA", url = "wss://relay.example").copy(isSearchEnabled = true, isSearchActive = true))`, and `advanceUntilIdle()`. Assert the call log is exactly the single enter marker. Complete the gate, `advanceUntilIdle()`, and assert the call log is two complete, strictly non-overlapping enter/exit pairs. Assert on ordering only — the non-empty-id save branch writes the caller's relay wholesale rather than merging, so the setter's flag is legitimately overwritten by the later save and a no-lost-update assertion would be wrong here.

Name the second exactly: `given deleteRelay overlapping a role setter on the same relay id when both resolve then the removal waits for the role write`. Same seeding and gate, call `coordinator.setOutboxEnabled("relayA", enabled = true)` then `coordinator.deleteRelay("relayA")`, `advanceUntilIdle()`, and assert the repository still returns a non-null row for that id — the removal is waiting on the lock. Complete the gate, `advanceUntilIdle()`, and assert the repository now returns null for that id and that the call log shows the setter's write completed.

Both cases must call the setter first so the gated invocation is the setter's own write.
  </action>
  <reversibility rating="reversible">Adds two test cases; no production behavior changes.</reversibility>
  <verify>
    <automated>./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest" &amp;&amp; ./gradlew compileDebugKotlin lintDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"` passes with ten cases executed (five pre-existing plus five added across this plan).
    - Both new method names match the specified strings character for character.
    - The save case asserts a single enter marker while the gate is held, and two complete non-overlapping pairs afterwards.
    - The delete case asserts the row is still present while the gate is held and absent afterwards.
    - `./gradlew lintDebug` succeeds with no new warnings.
    - `git status --porcelain app/src/main` is empty — no production file changed anywhere in this plan.
  </acceptance_criteria>
  <done>The save and delete paths are pinned to serialize against role writes on the same relay id, and all five new method names are recorded in the SUMMARY for citation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Relay configuration writes → persisted relay table | The relay records under test decide which relays the app dials and which appear in published relay-list events |
| Test evidence → validated-fix claim | These new cases become the citations that let three entries be recorded as validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04-01 | Repudiation | new test cases as evidence | medium | mitigate | Each case is specified to assert a state that differs before and after its fix — a cleared discovered flag, a blocked second write, a still-present row — rather than a final state a pre-fix implementation would also produce. |
| T-03-04-02 | Tampering | existing cases in the same file | medium | mitigate | The plan forbids renaming or deleting the five existing cases, which another plan cites by exact name, and every task's acceptance criteria assert the total executed case count. |
| T-03-04-03 | Denial of Service | per-relay lock held across the gate | low | accept | The gate is a test-only construct on a recording fake; production holds the lock only across a local read-map-persist sequence with no network suspension point. |
| T-03-04-04 | Information Disclosure | test data | low | accept | Test relays use example URLs and a synthetic pubkey; no real relay URL, pubkey, or user content appears. |
| T-03-04-SC | Tampering | npm/pip/cargo/Gradle installs | n/a | accept | No dependency is added; the package-legitimacy gate does not apply. |

No production code changes and no new attack surface: this plan adds unit tests only.
</threat_model>

<verification>
- `./gradlew testDebugUnitTest --tests "com.umbra.app.ui.relay.RelayCrudCoordinatorTest"` passes with ten cases.
- `./gradlew compileDebugKotlin` and `./gradlew lintDebug` both succeed with no new warnings.
- `git status --porcelain app/src/main` is empty.
- The five new method names are recorded verbatim in the plan SUMMARY so Plan 03-07 can cite them.

</verification>

<success_criteria>
- LOG-14, LOG-37, and LOG-42 each have a named, passing case that fails against their pre-fix behaviour.
- The existing five cases in the file remain untouched and passing.
</success_criteria>

## Artifacts this phase produces

New or changed symbols introduced by this plan (so drift verification does not flag them):

| Symbol | Kind | Location |
|--------|------|----------|
| `given a discovered relay when saveRelay assigns it an owned role then the persisted relay is no longer discovered` | new test method | `app/src/test/java/com/umbra/app/ui/relay/RelayCrudCoordinatorTest.kt` |
| `given a discovered relay already in the repository when saveRelay is called with a blank id and the same url then the merged row is no longer discovered` | new test method | same file |
| `given removeRelayRole overlapping a role setter on the same relay when both resolve then neither the removal nor the flag flip is lost` | new test method | same file |
| `given saveRelay overlapping a role setter on the same relay id when both resolve then the second write waits for the first` | new test method | same file |
| `given deleteRelay overlapping a role setter on the same relay id when both resolve then the removal waits for the role write` | new test method | same file |

No new production class, no new test class, no new fake, no new Gradle dependency.

<output>
Create `.planning/phases/03-fix-validation-test-coverage/03-04-SUMMARY.md` when done.
</output>
